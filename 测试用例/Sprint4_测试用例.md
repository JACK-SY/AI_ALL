# S4测试用例-v2.8-20260721

> 来源：v2.7基础上补充F12组合筛选与F15代理地址边界用例，并修正F1后端权限验证及F14限额拒绝日志口径 | 总用例数：153

***

## 模块概览

| 模块  | 模块名称               | 用例数 | P0 | P1 | P2 | P3 |
| --- | ------------------ | --- | -- | -- | -- | -- |
| F1  | 平台资源池与租户授权范围       | 33  | 9  | 18 | 6  | 0  |
| F2  | 接入点渠道锁定与路由意图       | 10  | 2  | 7  | 1  | 0  |
| F5  | 调用日志存储治理           | 8   | 1  | 5  | 2  | 0  |
| F6  | 用量视角与用户级Token限额    | 20  | 7  | 12 | 1  | 0  |
| F7  | 离职资源清理             | 7   | 2  | 5  | 0  | 0  |
| F8  | AI安全护栏接入点策略        | 25  | 3  | 22 | 0  | 0  |
| F12 | 接入凭证列表筛选增强         | 4   | 0  | 4  | 0  | 0  |
| F13 | CC Switch一键填入      | 9   | 2  | 7   | 0  | 0  |
| F14 | 用量统计多维度查询与调用日志展示增强 | 13  | 3  | 7  | 3  | 0  |
| F15 | Vertex供应商          | 11  | 1  | 7  | 3  | 0  |
| F16 | i18n多语言错误码         | 13  | 2  | 9  | 2  | 0  |
| 合计  | <br />             | 153 | 32 | 103 | 18 | 0  |

***

## F1 F1

### TC\_RES\_001

标题：【#F1\_RES\_授权范围】正向：资源授权范围页配置模型授权给租户

优先级：P0

前置条件：

> 1.平台管理员已登录;
> 2.模型库中存在已纳管模型glm-5.1;
> 3.目标租户CL0100尚未在模型M的授权范围内

测试步骤：

1.进入运营管理→资源授权范围，定位模型glm-5.1，打开授权管理抽屉;
2.配置可访问范围模式为指定范围（SPECIFIED），搜索租户CL0100并勾选授权;
3.点击保存

预期结果：

1.搜索结果展示CL0100及租户名称;
2.提示保存成功;
3.租户CL0100内用户可在模型广场看到模型glm-5.1，可创建模型级API Key，可直调model=glm-5.1

***

### TC\_RES\_002

标题：【#F1\_RES\_推式投放】异常：搜索框特殊字符输入处理

优先级：P2

前置条件：

> 1.平台管理员已登录;
>
> 2.进入运营管理→资源授权范围

测试步骤：

1.在资源搜索框输入\_或%，观察搜索结果和页面行为;

2.打开模型M的授权管理抽屉，在租户搜索框输入\_或%，观察搜索结果和页面行为。

预期结果：

1.搜索结果不会模糊全匹配

***

### TC\_RES\_003

标题：【#F1\_RES\_授权范围】正向：授权完成后独立调整授权范围增减租户

优先级：P1

前置条件：

> 1.平台管理员已登录;
>
> 2.模型M已在资源授权范围页授权给CL0100（指定范围/SPECIFIED模式）

测试步骤：

1.进入运营管理→资源授权范围，定位模型M;

2.打开授权管理抽屉，新增租户CL0300授权;

3.移除租户CL0100授权，点击保存

预期结果：

1.提示保存成功;

2.CL0300内用户可在模型广场看到模型M，可调用;

3.CL0100内用户模型广场不展示模型M，直调返回RESOURCE\_NOT\_AUTHORIZED;

***

### TC\_RES\_004

标题：【#F1\_RES\_撤销接入点授权】正向：接入点撤销授权后绑定失效+调用拒绝+展示失效原因

优先级：P1

前置条件：

> 1.ep-public已在CL0100授权范围内;
>
> 2.userA拥有绑定ep-public的API Key

测试步骤：

1.平台管理员撤销CL0100对ep-public的授权;

2.userA查看API Key详情;

3.userA调用model=ep-public;

4.userA编辑API Key尝试绑定ep-public

预期结果：

1.API Key详情中ep-public绑定展示「平台撤销授权」失效原因及产生时间;

2.调用被拒绝，返回RESOURCE\_NOT\_AUTHORIZED;

3.选择器中不展示ep-public

***

### TC\_RES\_005

标题：【#F1\_RES\_授权范围汇总】交互：资源授权范围页汇总计数展示

优先级：P2

前置条件：

> 1.平台管理员登录运营管理→资源授权范围;
>
> 2.存在ALL（全部）模式资源1个（含黑名单1个租户）、SPECIFIED（指定范围）模式资源3个,白名单分别授权1、2、3个租户

测试步骤：

1.查看资源授权「全部可访问」范围；

2.查看「指定范围」模式资源。

预期结果：

1.可访问范围为「全部」，黑名单数列展示1;

2.白名单数列分别展示对应租户数量为1、2、3（分列独立展示）。

***

### TC\_RES\_006

标题：【#F1\_RES\_撤权确认】交互：撤销授权/封禁二次确认弹窗

优先级：P2

前置条件：

> 1.平台管理员登录运营管理→资源授权范围;
>
> 2.模型M已授权给CL0100，关联有效API Key数为
>
> 3.关联业务接入点候选数为2

测试步骤：

1.定位模型M（指定范围/SPECIFIED模式），打开授权管理抽屉，移除CL0100白名单授权;

2.查看撤权二次确认弹窗内容;

3.填写确认文案点击确定;

4.填写后点击确定

预期结果：

1.弹窗展示影响清单：关联有效API Key数=3、业务接入点候选引用数=2、最近30天调用次数、最近调用时间;

2.说明文案含撤销后绑定进入失效状态、调用被拒绝等信息;

3.确认失败，提示必须确认;

4.确认成功，
撤销授权/封禁成功

***

### TC\_RES\_007

标题：【#F1\_RES\_重复授权】正向：重复授权场景

优先级：P1

前置条件：

> 1.模型M已授权给租户CL0100（指定范围/SPECIFIED模式）

测试步骤：

1.平台管理员在资源授权范围页面再次将模型M授权给CL0100;

2.验证授权记录是否产生重复

预期结果：

1.系统拒绝重复授权，提示等效于"租户CL0100已在授权范围内"等效提示或页面授权成功；

2.不产生重复授权记录

***

### TC\_RES\_008

标题：【#F1\_RES\_业务接入点约束】正向：业务接入点编辑时只展示授权范围内模型可选

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录
> 2. CL0100授权范围内包含模型M1，不包含模型M2（或M2被封禁）
> 3. 存在业务接入点ep-biz

测试步骤：

1. 进入接入点管理→点击ep-biz→编辑
2. 点击"添加候选"
3. 查看候选选择器列表

预期结果：

1. 候选选择器中展示模型M1(providerId+modelId)
2. 模型M2不在选择器中展示（未授权或被封禁）
3. 已有的M2候选（如历史上添加的）展示为不可用/异常状态

***

### TC\_RES\_009

标题：【#F1\_RES\_授权独立组合】正向：象限1-接入点已授权+模型已授权

优先级：P1

前置条件：

> 1. 公共接入点 ep-public（候选含模型 M）和模型 M 均已授权给 CL0100；
> 2. 普通用户 userA 拥有绑定 ep-public 的 API Key Key1 和绑定模型 M 的 API Key Key2

测试步骤：

1. userA 使用 Key1 调用 model=ep-public；
2. userA 使用 Key2 调用 model=glm-5.1

预期结果：

1. Key1 调用成功；
2. Key2 调用成功

***

### TC\_RES\_010

标题：【#F1\_RES\_授权独立组合】正向：象限2-接入点已授权+模型未授权

优先级：P1

前置条件：

> 1. 公共接入点 ep-public 已授权给 CL0100，模型 M 未授权；
> 2. 普通用户 userA 拥有绑定 ep-public 的 API Key Key1 和绑定模型 M 的 API Key Key2

测试步骤：

1. userA 使用 Key1 调用 model=ep-public；
2. userA 使用 Key2 调用 model=glm-5.1

预期结果：

1. Key1 调用成功（ep-\* 调用只校验接入点授权，内部路由候选模型不过授权层）；
2. Key2 调用被拒绝

***

### TC\_RES\_011

标题：【#F1\_RES\_授权独立组合】正向：象限3-接入点未授权+模型已授权

优先级：P1

前置条件：

> 1. 模型 M 已授权给 CL0100，公共接入点 ep-public 未授权；
> 2. 普通用户 userA 拥有绑定 ep-public 的 API Key Key1 和绑定模型 M 的 API Key Key2

测试步骤：

1. userA 使用 Key1 调用 model=ep-public；
2. userA 使用 Key2 调用 model=glm-5.1

预期结果：

1. Key1 调用被拒绝，展示「平台撤销授权」；
2. Key2 调用成功

***

### TC\_RES\_012

标题：【#F1\_RES\_授权独立组合】正向：象限4-接入点未授权+模型未授权

优先级：P1

前置条件：

> 1. 公共接入点 ep-public 和模型 M 均未授权给 CL0100；
> 2. 普通用户 userA 拥有绑定 ep-public 的 API Key Key1 和绑定模型 M 的 API Key Key2

测试步骤：

1. userA 使用 Key1 调用 model=ep-public；
2. userA 使用 Key2 调用 model=glm-5.1

预期结果：

1. Key1 调用被拒绝；
2. Key2 调用被拒绝

***

### TC\_RES\_013

标题：【#F1\_RES\_撤销再授权恢复】边界：撤销授权后重新授权应自动恢复失效绑定

优先级：P1

前置条件：

> 1.平台管理员已登录
>
> 2.模型M已授权给租户CL0100（指定范围/SPECIFIED模式）
>
> 3.userA拥有绑定模型M的API Key，当前绑定处于有效状态

测试步骤：

1.平台管理员撤销CL0100对模型M的授权
，确认userA的API Key绑定展示「平台撤销授权」；

2.平台管理员重新授权模型M给CL0100
；

3.查看userA的API Key绑定状态和调用能力。

预期结果：

1.撤销后绑定失效，展示「平台撤销授权」失效原因
；

2.重新授权后历史失效绑定自动恢复有效
；

3.userA可正常调用模型M
并记录调用日志。

***

### TC\_RES\_014

标题：【#F1\_RES\_租户权限约束】正向：租户管理员不可自行将未授权资源加入授权范围

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录；
> 2. 平台模型库中存在模型M；
> 3. 模型M未授权给租户CL0100

测试步骤：

1. 进入模型广场并搜索模型M；
2. 进入接入点编辑页并展开候选模型选择器；
3. 通过浏览器地址直接访问平台资源授权范围管理页面；
4. 使用CL0100租户管理员的登录凭证调用授权管理接口，尝试将模型M授权给CL0100；
5. 记录接口HTTP状态码和错误响应；
6. 重新进入模型广场并搜索模型M；
7. 重新进入接入点编辑页并展开候选模型选择器；
8. 使用CL0100名下API Key直调模型M

预期结果：

1. 模型广场不展示模型M；
2. 候选模型选择器不展示模型M；
3. 直接访问授权管理页面时返回无权限页面或重定向，不展示可操作的授权控件；
4. 授权管理接口返回403或等价权限拒绝错误，不返回授权成功结果；
5. 接口拒绝后模型M仍不出现在模型广场和候选模型选择器中；
6. 使用CL0100名下API Key直调模型M返回RESOURCE_NOT_AUTHORIZED或等价未授权错误；
7. 越权接口调用未改变CL0100对模型M的授权状态

***

### TC\_RES\_015

标题：【#F1\_RES\_租户权限约束】正向：租户管理员不可修改公共接入点AI安全护栏策略

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录；
> 2. CL0100授权范围中存在公共接入点ep-public；
> 3. ep-public已套用AI安全护栏推荐配置；
> 4. ep-public当前已套用推荐配置并处于观察模式

测试步骤：

1. 进入公共接入点ep-public详情页；
2. 查看AI安全护栏配置区块；
3. 尝试切换观察模式为拦截模式；
4. 使用CL0100租户管理员的登录凭证调用护栏策略修改接口，尝试切换拦截模式或替换推荐配置；
5. 记录接口HTTP状态码和错误响应；
6. 刷新ep-public详情页；
7. 重新查看护栏模式和推荐配置状态；
8. 使用有权限的API Key向ep-public发送高风险请求；
9. 查看调用结果及护栏检测记录

预期结果：

1. 护栏配置区块为只读展示，不存在可提交的模式修改控件；
2. 页面不存在自定义标签、检测范围、阈值、风险映射或处置规则入口；
3. 护栏策略修改接口返回403或等价权限拒绝错误，不返回修改成功结果；
4. 刷新后ep-public仍为观察模式；
5. 刷新后推荐配置和观察模式保持不变；
6. 高风险请求按原观察模式放行，调用日志记录护栏检测结果及处置动作为“记录”；
7. 越权接口调用未改变ep-public的护栏策略

***

### TC\_RES\_016

标题：【#F1\_RES\_筛选排序】交互：资源授权范围页筛选维度和排序切换验证

优先级：P1

前置条件：

> 1.平台管理员已登录
>
> 2.系统中存在多种可访问范围模式和授权状态的资源

测试步骤：

1.进入运营管理→资源授权范围（资源维度）;

2.测试筛选维度：资源类型（模型/公共接入点）、可访问范围模式（全部/指定范围）;

3.测试搜索功能：按资源ID和名称匹配;

预期结果：

1.筛选维度控件均可用，筛选结果与条件精确匹配;

2.搜索结果按资源ID/名称精确匹配;

3.列表展示白名单数、黑名单数列独立呈现

***

### TC\_RES\_017

标题：【#F1\_RES\_菜单权限】正向：授权范围管理入口仅平台管理员可操作

优先级：P2

前置条件：

> 1.准备租户CL0100管理员账号admin\_T和普通用户账号userA
>
> 2.准备平台管理员账号admin\_P

测试步骤：

1.以admin\_T登录系统，观察左侧导航菜单

2.以userA登录系统，观察左侧导航菜单

3.以admin\_P登录系统，观察左侧导航菜单

4.userA尝试通过URL直接访问资源管理页面

预期结果：

1.admin\_T在左侧导航菜单中看不到"资源管理"一级菜单入口

2.userA的左侧导航菜单中不展示"资源管理"入口

3.admin\_P的左侧导航菜单中以平台管理视角展示"资源管理"入口

4.userA、admin\_T通过URL直接访问返回权限拒绝或重定向至无权限页面

***

### TC\_RES\_018

标题：【#F1\_RES\_端到端】正向：模型授权配置与租户可见性验证

优先级：P0

前置条件：

> 1.平台管理员已登录
> 2.模型库存在已纳管模型M（providerId=ali，modelId=glm-5.1）
> 3.系统中存在租户CL0100和CL0200

测试步骤：

1.进入运营管理→资源授权范围，配置模型M可访问范围模式为指定范围（SPECIFIED），授权CL0100和CL0200，保存

预期结果：

1.模型M授权成功，CL0100/CL0200可见可调用
2.指定范围（SPECIFIED）模式生效，其他租户不可见

***

### TC\_RES\_019

标题：【#F1\_RES\_端到端】正向：公共接入点创建与授权

优先级：P0

前置条件：

> 1.模型M已授权给CL0100和CL0200

测试步骤：

1.进入接入点管理→新建公共接入点ep-glm-public，选择模型=ali+glm-5.1，保存
2.在资源授权范围页将ep-glm-public授权给CL0100和CL0200

预期结果：

1.公共接入点ep-glm-public创建成功
2.接入点授权成功，CL0100/CL0200可见可调用

***

### TC\_RES\_020

标题：【#F1\_RES\_端到端】正向：安全护栏配置与调用验证

优先级：P0

前置条件：

> 1.接入点ep-glm-public已授权给CL0100和CL0200

测试步骤：

1.进入ep-glm-public详情→安全护栏页签，套用推荐配置（观察模式），保存
2.CL0100普通用户userA创建API Key Key1，绑定模型M+接入点ep-glm-public
3.userA使用Key1直调模型M和调用接入点
4.userA发送高风险内容请求通过ep-glm-public
5.验证调用日志记录完整性

预期结果：

1.安全护栏配置为观察模式
2.userA直调和接入点调用均成功
3.高风险请求不被阻断（观察模式），公共接入点检测结果记录风险类型和等级
4.模型直调不产生护栏检测记录；公共接入点调用记录路由意图、实际命中模型和安全护栏检测结果

***

### TC\_RES\_021

标题：【#F1\_RES\_黑白名单叠加】正向：WHITELIST模式下黑名单优先生效

优先级：P0

前置条件：

> 1.模型M可访问范围模式为WHITELIST;
> 2.租户CL0100和CL0200在模型M的白名单内;
> 3.CL0100和CL0200均可正常调用模型M

测试步骤：

1.平台管理员将CL0100加入模型M的黑名单（保留白名单条目）;
2.使用CL0100租户的API Key调用模型M;
3.使用CL0200租户的API Key调用模型M;
4.平台管理员删除CL0100的黑名单条目（解封）;
5.使用CL0100租户的API Key调用模型M

预期结果：

1.黑名单添加成功，CL0100在模型M的白名单条目仍保留;
2.CL0100调用被拒绝，返回RESOURCE\_NOT\_AUTHORIZED错误码;
3.CL0200调用正常成功，不受影响;
4.黑名单删除成功（解封）;
5.CL0100调用恢复成功，白名单资格自动恢复，无需重新授权

***

### TC\_RES\_022

标题：【#F1\_RES\_动态权限】正向：全部资源权限API Key新增资源自动可用

优先级：P0

前置条件：

> 1.租户CL0100已创建"全部资源权限"的API Key KeyAll;
> 2.KeyAll当前可调用模型M1和M2;
> 3.模型M3尚未授权给CL0100租户

测试步骤：

1.使用KeyAll调用模型M1，验证调用成功;
2.平台管理员将模型M3授权给CL0100租户;
3.使用KeyAll调用模型M3，验证调用成功;
4.使用KeyAll调用模型M1，验证调用仍然成功;
5.平台管理员撤销模型M3对CL0100的授权;
6.使用KeyAll调用模型M3，验证调用被拒绝

预期结果：

1.KeyAll调用M1成功;
2.模型M3授权成功，进入CL0100授权范围;
3.KeyAll调用M3成功，无需更新Go token即可使用新授权资源;
4.KeyAll调用M1仍然成功，不受授权变更影响;
5.模型M3撤权成功;
6.KeyAll调用M3被拒绝，返回RESOURCE\_NOT\_AUTHORIZED错误码

***

### TC\_AUTH\_001

标题：【#F1\_AUTH\_黑名单封禁】正向：ALL模式资源对滥用租户加入黑名单后不可见不可调用

优先级：P0

前置条件：

> 1.平台管理员已登录;
>
> 2.模型M可见范围模式为ALL，当前全部租户可见;
>
> 3.租户CL0500近期滥用模型M

测试步骤：

1.进入运营管理→资源授权范围，定位模型M;

2.打开授权管理抽屉，将CL0500加入黑名单（填写封禁原因reason）;

3.确认二次确认弹窗;

4.以CL0500普通用户登录验证

预期结果：

1.黑名单添加成功;

2.CL0500用户模型广场不展示模型M，列表/筛选/选择器均不出现;

3.已有API Key中模型M绑定展示「平台撤销授权」失效原因;

4.直调model=glm-5.1返回RESOURCE\_NOT\_AUTHORIZED;

5.其他租户不受影响;

***

### TC\_AUTH\_002

标题：【#F1\_AUTH\_黑名单解封（ALL模式）】正向：解封（删黑名单条目）后租户恢复可访问

优先级：P1

前置条件：

> 1.模型M为ALL（全部可访问）模式，CL0100被加入黑名单（被封禁状态）;
>
> 2.userA有绑定模型M的API Key，当前展示「平台撤销授权」

测试步骤：

1.平台管理员删除CL0100的黑名单条目（解封）;

2.userA查看API Key详情;

3.userA使用该API Key调用model=glm-5.1

预期结果：

1.解封成功;

2.API Key绑定自动恢复有效;

3.调用成功;

4.**注意：SPECIFIED（指定）模式不允许添加黑名单，黑白互斥**

***

### TC\_AUTH\_003

标题：【#F1\_AUTH\_三层校验顺序】正向：授权范围→API Key权限→既有前置校验

优先级：P0

前置条件：

> 1.模型M已授权给CL0100（授权范围通过）;
>
> 2.userA的API Key Key1绑定模型M（API Key权限通过）;
>
> 3.模型M当前状态正常

测试步骤：

1.userA使用Key1调用model=glm-5.1（三层全通过）;

2.平台管理员撤销CL0100对模型M的授权;

3.userA使用Key1再次调用model=glm-5.1;

4.平台管理员重新授权模型M给CL0100;

5.解除Key1对模型M的绑定（模拟API Key权限不足）;

6.userA使用Key1调用model=glm-5.1

预期结果：

1.三层全通过时调用成功;

2.授权层被拒后返回RESOURCE\_NOT\_AUTHOR栏IZED（第一层失败立即返回）;

3.重新授权后，授权层恢复通过;

4.API Key权限不足时返回RESOURCE\_NOT\_IN\_KEY\_PERMISSION（第二层失败）;

***

### TC\_AUTH\_004

标题：【#F1\_AUTH\_同码屏蔽】正向：黑名单拒绝与未授权对外同码RESOURCE\_NOT\_AUTHORIZED

优先级：P1

前置条件：

> 1.模型M为指定范围（SPECIFIED）模式，CL0200不在白名单中;
>
> 2.模型M为ALL（全部可访问）模式，CL0300在黑名单中

测试步骤：

1.CL0200普通用户调用model=glm-5.1;

2.CL0300普通用户调用model=glm-5.1;

3.对比两次返回的错误码

预期结果：

1.CL0200返回RESOURCE\_NOT\_AUTHORIZED;

2.CL0300返回RESOURCE\_NOT\_AUTHORIZED;

3.两者对外同码，调用方无法区分「从未授权」与「被封禁」;

4.封禁事实（黑名单标记与reason）仅平台侧审计与授权页可见

***

### TC\_AUTH\_005

标题：【#F1\_AUTH\_存量升级不变】正向：存量公共资源升级后行为不变

优先级：P1

前置条件：

> 1.升级前系统中已有存量公共模型M和公共接入点ep-old，均未设置可见范围模式和黑名单

测试步骤：

1.升级后以已有租户用户登录;

2.查看模型M和ep-old的可见性和调用能力;

预期结果：

1.存量资源视同ALL模式、无黑名单条目;

2.所有租户可见性调用能力不变

***

### TC\_AUTH\_006

标题：【#F1\_AUTH\_候选模型授权提示】正向：接入点行展示候选模型授权状态信息提示

优先级：P1

前置条件：

> 1.公共接入点ep-public候选含模型M（ali+glm-5.1）;
> 2.模型M已授权给CL0100，未授权给CL0200

测试步骤：

1.进入运营管理→资源授权范围，定位ep-public;

2.查看ep-public行信息提示

预期结果：

1.ep-public行展示可访问范围（全部/指定范围）+ 白名单数、黑名单数列独立展示;

2.接入点行展示背后候选模型M的授权状态提示（信息提示，不做校验拦截）;

3.CL0200仍可通过ep-public调用（接入点授权独立于候选模型）

***

### TC\_AUTH\_007

标题：【#F1\_AUTH\_模式双向切换】正向：ALL↔SPECIFIED双向切换

优先级：P0

前置条件：

> 1.模型M当前可访问范围模式为ALL（全部租户可见）

测试步骤：

1.将模型M从ALL切换为SPECIFIED，预填最近30天有调用租户（既有黑名单自动清空）;

2.调整白名单后保存;

3.将模型M从SPECIFIED切回ALL;

4.保存

预期结果：

1.ALL→SPECIFIED切换成功，未在白名单的租户全链路不可见;

2.SPECIFIED→ALL切换成功，全部租户恢复可见;

3.ALL→SPECIFIED切换时既有黑名单条目自动清空（弹窗明示），SPECIFIED→ALL白名单条目保留休眠;

***

### TC\_AUTH\_008

标题：【#F1\_AUTH\_封禁原因字段】正向：黑名单reason字段正确记录和展示

优先级：P2

前置条件：

> 1.平台管理员已登录

测试步骤：

1.将租户CL0100加入模型M的黑名单，不填写填写reason，尝试保存;

2.填写reason="滥用调用导致成本异常"保存;

3.在资源授权范围页查看黑名单条目

预期结果：

1.reason字段选填，填写后正确写入;

2.资源授权范围页黑名单区域展示reason;

***

### TC\_AUTH\_009

标题：【#F1\_AUTH\_黑名单封禁确认弹窗（ALL模式）】正向：封禁操作二次确认弹窗展示影响清单

优先级：P2

前置条件：

> 1.平台管理员已登录;
>
> 2.模型M为ALL（全部可访问）模式;
>
> 3.模型M关联CL0100的有效API Key数为5，关联业务接入点候选引用数为3，最近30天调用次数>100

测试步骤：

1.进入运营管理→资源授权范围，定位模型M;

2.打开授权管理抽屉，将CL0100加入黑名单（填写reason="异常调用成本过高"）;

3.确认二次确认弹窗

预期结果：

1.弹窗展示影响清单：关联有效API Key数=5、业务接入点候选引用数=3、最近30天调用次数、最近调用时间;

2.说明文案含加入黑名单后租户不可见不可调用、已有绑定展示「平台撤销授权」等信息;

3.不勾选确认点击确定→按钮禁用;

4.勾选后点击确定→封禁成功;

5.**注意：SPECIFIED（指定范围）模式不允许添加黑名单，黑白互斥**

***

### TC\_AUTH\_010

标题：【#F1\_AUTH\_封禁解封模式切换缓存失效】正向：封禁/解封/模式切换后事件驱动缓存失效

优先级：P1

前置条件：

> 1.模型M为ALL（全部可访问）模式，CL0100未被封禁;
>
> 2.授权范围校验基于缓存执行

测试步骤：

1.userA调用model=glm-5.1验证可调用（基准）;

2.平台管理员将CL0100加入模型M的黑名单;

3.userA下一次调用验证结果;

4.平台管理员删除CL0100的黑名单条目（解封）;

5.userA下一次调用验证结果;

6.平台管理员将模型M从ALL切换为SPECIFIED（白名单预填CL0100）;

7.验证CL0100是否仍可见，其他租户是否不可见

预期结果：

1.封禁后下一次调用被拒绝（缓存事件驱动失效，无需等待自然过期）;

2.解封后下一次调用恢复成功;

3.ALL→SPECIFIED模式切换后，仅白名单租户可见，其他租户不可见;

4.三次操作均触发事件驱动缓存失效;

5.**注意：SPECIFIED模式不允许添加黑名单**

***

### TC_AUTH_011

标题：【#F1_AUTH_端到端】正向：微调大模型授权→调用→封禁→解封全流程验证

优先级：P1

前置条件：

> 1.平台管理员已登录；
> 2.系统存在微调大模型M（glm-5.1），当前可访问范围模式为ALL；
> 3.系统存在租户CL0100和普通用户userA；
> 4.userA在CL0100下尚未创建绑定模型M的API Key

测试步骤：

1.平台管理员进入资源授权范围页，确认模型M为ALL模式且无黑名单；
2.userA登录CL0100，进入模型广场查看模型M；
3.userA创建API Key Key1并绑定模型M；
4.userA使用Key1调用model=glm-5.1，验证调用成功；
5.平台管理员将CL0100加入模型M的黑名单，填写封禁原因"滥用调用"；
6.userA再次使用Key1调用model=glm-5.1；
7.userA查看模型广场和API Key绑定状态；
8.平台管理员删除CL0100的黑名单条目（解封）；
9.userA使用Key1调用model=glm-5.1，验证调用恢复成功；
10.平台管理员将模型M从ALL切换为SPECIFIED，白名单预填CL0100；
11.userA验证仍可调用，其他租户验证不可见

预期结果：

1.模型M为ALL模式，所有租户可见；
2.userA在模型广场可见模型M；
3.Key1创建成功并绑定模型M；
4.调用成功，返回正常响应；
5.黑名单添加成功，二次确认弹窗展示影响清单；
6.调用返回RESOURCE_NOT_AUTHORIZED错误码；
7.模型广场不展示模型M，Key1绑定展示「平台撤销授权」失效原因；
8.解封成功，黑名单条目删除；
9.调用恢复成功，无需重新生成API Key；
10.模式切换成功，白名单预填CL0100；
11.CL0100仍可调用模型M，其他租户不可见不可调用

***

## F2 F2

### TC\_EP\_001

标题：【#F2\_EP\_渠道锁定】正向：同名模型不同供应商各自命中指定渠道

优先级：P0

前置条件：

> 1.模型glm-5.1来自ali和qwen两个供应商

测试步骤：

1.创建ep-glm-ali候选锁定ali+glm-5.1;

2.创建ep-glm-qwen候选锁定qwen+glm-5.1;

3.分别调用

预期结果：

1.ep-glm-ali稳定命中ali;

2.ep-glm-qwen稳定命中qwen;

3.互不干扰

***

### TC\_EP\_002

标题：【#F2\_EP\_指定模型】异常：候选不可用时调用失败不切换

优先级：P1

前置条件：

> 1.接入点ep-glm-ali唯一候选ali+glm-5.1;
> 2.ali供应商健康状态异常;
> 3.存在同名IDqwen + glm-5.1

测试步骤：

1.使用绑定ep-glm-ali的APIKey调用model=ep-glm-ali

预期结果：

1.调用失败返回网关通用错误码;
2.不自动切换到qwen同名模型;
3.日志记录调用失败

***

### TC\_EP\_003

标题：【#F2\_EP\_存量回填】正向：单渠道候选自动回填

优先级：P1

前置条件：

> 1.存在老接入点ep-old候选modelId=glm-5.1无providerId;
> 2.该modelId只有一个可用渠道ali

测试步骤：

1.执行存量回填;
2.检查ep-old候选映射记录

预期结果：

1.候选记录providerId更新为ali;
2.绑定键变为ali+glm-5.1;
3.回填日志记录

***

### TC\_EP\_004

标题：【#F2\_EP\_选择器】交互：选择器列结构替换验证

优先级：P2

前置条件：

> 1.租户CL0100管理员已登录;
> 2.拥有可编辑的APIKey

测试步骤：

1.编辑APIKey→模型直连(高级)标签;
2.检查候选选择器列结构

预期结果：

1.原有模型名称列已替换为供应商名称列;
2.列结构为模型code/供应商名称/模型规格;
3.同名modelId来自不同供应商时逐行独立展示

***

### TC\_EP\_005

标题：【#F2\_EP\_选择器】正向：未授权/被封禁资源不在选择器展示

优先级：P1

前置条件：

> 1.租户CL0100授权范围内包含ali+glm-5.1，不包含qwen+glm-5.1（未授权或被封禁）

测试步骤：

1.编辑API Key→模型直连（高级）标签;
2.查看候选选择器列表

预期结果：

1.选择器中只展示ali+glm-5.1;
2.qwen+glm-5.1不在选择器中展示（未授权或被封禁的资源租户侧完全不可见）

***

### TC\_EP\_006

标题：【#F2\_EP\_租户只读】交互：公共接入点详情只读验证

优先级：P1

前置条件：

> 1.租户CL0100管理员已登录;
> 2.ep-public存在且已在资源池中

测试步骤：

1.进入接入点管理点击ep-public;
2.查看详情页内容;
3.尝试编辑路由或候选

预期结果：

1.详情页展示路由意图+默认模型摘要(modelId+providerName);
2.不展示完整候选列表/候选数量/供应商配置;
3.编辑按钮不存在或禁用

***

### TC\_EP\_007

标题：【#F2\_EP\_默认意图】正向：新建接入点默认路由意图为指定模型

优先级：P1

前置条件：

> 1. 平台管理员已登录；
> 2. 接入点管理页面可访问

测试步骤：

1. 点击新建接入点；
2. 填写基本信息（名称、归属类型=公共）；
3. 进入路由配置区域，查看路由意图默认值；
4. 不修改路由意图，添加候选 ali+glm-5.1；
5. 保存

预期结果：

1. 路由意图默认值为"指定模型"；
2. 保存后接入点路由意图字段为"指定模型"；
3. 调用 model=ep-xxx 稳定命中 ali+glm-5.1

***

### TC\_EP\_008

标题：【#F2\_EP\_路由字段】正向：调用日志记录路由相关字段完整性

优先级：P2

前置条件：

> 1. 存在指定模型接入点 ep-glm-ali，候选 ali+glm-5.1；
> 2. 拥有绑定该接入点的 API Key

测试步骤：

1. 使用 API Key 调用 model=ep-glm-ali；
2. 进入调用日志查询本次调用记录；
3. 检查以下字段： - 路由意图（routing\_intent） - 实际命中模型（actual\_model） - 实际命中供应商（actual\_provider） 

预期结果：

调用日志记录中路由相关字段均存在且值正确：

1\. routing\_intent = "指定模型"；

2.actual\_model = "glm-5.1"；

3.actual\_provider = "ali"；

<br />

***

### TC\_EP\_009

标题：【#F2\_EP\_创建编辑业务接入点】正向：租户管理员创建并编辑业务接入点

优先级：P0

前置条件：

> 1.租户CL0100管理员已登录;
> 2.CL0100授权范围内存在模型M1（ali+glm-5.1）

测试步骤：

1.进入接入点管理→点击新建接入点;
2.填写基本信息，选择类型为业务接入点;
3.路由意图选择指定模型，候选选择ali+glm-5.1;
4.保存;
5.编辑该业务接入点，新增候选（如有其他授权模型）

预期结果：

1.创建成功;
2.业务接入点仅CL0100内用户可见;
3.编辑保存成功，候选范围不超过授权范围

***

### TC\_EP\_010

标题：【#F2\_EP\_租户列表合并筛选】正向：租户侧一个列表展示公共和业务接入点并按类型筛选

优先级：P1

前置条件：

> 1.租户CL0100管理员已登录;
> 2.系统中存在已授权给CL0100的公共接入点ep-public和CL0100自建的业务接入点ep-biz

测试步骤：

1.进入接入点管理页面;
2.查看列表内容;
3.使用类型筛选控件筛选"公共接入点";
4.筛选"业务接入点"

预期结果：

1.列表同时展示ep-public和ep-biz;
2.每条记录有类型标识区分;
3.筛选公共接入点时仅展示ep-public;
4.筛选业务接入点时仅出示ep-biz

***

## F5 F5

### TC\_LOG\_001

标题：【#F5\_LOG\_列表页不展示原文】正向：调用日志列表页不展示请求/响应原文

优先级：P1

前置条件：

> 1.租户管理员已登录;
> 2.存在多条调用日志记录（包含不同模型、状态的调用）

测试步骤：

1.进入模型观测→调用日志列表页;
2.查看列表中的字段展示

预期结果：

1.列表页仅展示查询索引字段（request\_id、modelId、providerId、status、tokens\_total、latency等）;
2.列表中不展示请求/响应原文内容;
3.列表中不展示"查看原文"按钮

***

### TC\_LOG\_002

标题：【#F5\_LOG\_详情页查看原文】正向：调用日志详情页查看原文

优先级：P0

前置条件：

> 1.租户管理员已登录;
> 2.存在一条调用成功的日志记录，且content\_write\_status=SUCCESS

测试步骤：

1.进入模型观测→调用日志列表页;
2.点击某条调用日志进入详情页;
3.点击"查看原文"按钮

预期结果：

1.详情页展示该调用的基本信息（模型、状态、Token用量等）;
2.详情页显示"查看原文"按钮;
3.点击后展开显示请求原文和响应原文;
4.原文展示区域包含"请求体"和"响应体"两个标签页

***

### TC\_LOG\_003

标题：【#F5\_LOG\_原文加载状态】交互：原文加载中状态提示

优先级：P1

前置条件：

> 1.租户管理员已登录;
> 2.存在一条调用日志，原文数据较大

测试步骤：

1.进入调用日志详情页;
2.快速点击"查看原文"按钮

预期结果：

1.点击后展示"正在加载调用原文..."提示;
2.加载完成后自动显示原文内容;
3.加载过程中按钮变为禁用状态

***

### TC\_LOG\_004

标题：【#F5\_LOG\_原文写入失败不展示】正向：内容写入失败时不展示查看原文按钮

优先级：P1

前置条件：

> 1.租户管理员已登录;
> 2.存在一条调用日志，content\_write\_status=FAILED

测试步骤：

1.进入调用日志详情页;
2.查看页面是否有"查看原文"按钮

预期结果：

1.详情页展示调用基本信息正常;
2.页面不展示"查看原文"按钮;
3.页面展示提示"内容写入失败，无法查看原文"

***

### TC\_LOG\_005

标题：【#F5\_LOG\_原文过期提示】正向：内容索引过期后原文查看提示

优先级：P1

前置条件：

> 1.租户管理员已登录;
> 2.存在一条超过30天的调用日志，内容索引已过期删除

测试步骤：

1.进入调用日志详情页;
2.点击"查看原文"按钮（若存在）

预期结果：

1.若存在"查看原文"按钮，点击后展示"该调用原文已超过保留期限，不可追溯";
2.若不存在按钮，页面展示提示"该调用原文已超过保留期限，不可追溯";
3.调用基本信息仍可正常查看

***

### TC\_LOG\_006

标题：【#F5\_LOG\_存量数据兼容】正向：存量数据（无content\_ref）原文查看

优先级：P1

前置条件：

> 1.租户管理员已登录;
> 2.存在一条存量调用日志（无content\_ref字段，直接存储在kmo\_call\_log索引中）

测试步骤：

1.进入调用日志详情页;
2.查看原文展示情况

预期结果：

1.详情页展示调用基本信息正常;
2.页面展示"查看原文"按钮;
3.点击后可正常查看请求/响应原文（存量数据兼容展示）

***

### TC\_LOG\_007

标题：【#F5\_LOG\_跨月查询】正向：跨月时间范围查询展示

优先级：P2

前置条件：

> 1.租户管理员已登录;
> 2.存在跨两个月的调用日志数据

测试步骤：

1.进入调用日志列表页;
2.设置时间范围跨越两个月份（如6月15日至7月15日）;
3.点击查询

预期结果：

1.查询结果同时包含6月和7月的数据;
2.列表中各条日志的时间戳正确显示对应月份;
3.点击任一条日志均可正常进入详情页

***

### TC\_LOG\_008

标题：【#F5\_LOG\_超期数据查询】边界：超过12个月保留期的数据查询

优先级：P2

前置条件：

> 1.租户管理员已登录;
> 2.系统中存在超过12个月的历史调用数据

测试步骤：

1.进入调用日志列表页;
2.设置时间范围包含超过12个月前的数据（如13个月前至今）;
3.点击查询

预期结果：

1.超过12个月的数据不展示在列表中;
2.列表只展示12个月内的调用日志;
3.无错误提示，查询结果正常返回

***

## F6 F6

### TC\_QUO\_001

标题：【#F6\_QUO\_视角】正向：普通用户仅查看我的用量

优先级：P1

前置条件：

> 1.普通用户userA\@CL0100已登录;
> 2.userA名下有2把APIKey

测试步骤：

1.进入用量统计页面;
2.观察页面标题和视角标识;
3.尝试查找视角切换控件

预期结果：

1.页面展示我的用量;
2.当前视角标签明确展示我的用量;
3.无视角切换控件

***

### TC\_QUO\_002

标题：【#F6\_QUO\_视角】正向：租户管理员视角切换

优先级：P0

前置条件：

> 1.租户管理员admin\@CL0100已登录;
> 2.本租户有多个用户产生用量

测试步骤：

1.进入用量统计页面;
2.观察默认视角;
3.切换到我的用量;
4.切换回本租户用量

预期结果：

1.默认展示本租户用量;
2.切换后展示我的用量;
3.切换回恢复全租户数据

***

### TC\_QUO\_003

标题：【#F6\_QUO\_限额拦截】正向：单独配额用户达额度拦截

优先级：P0

前置条件：

> 1.平台管理员为userA\@CL0100配置额度=100Mtokens/月;
> 2.userA已用95Mtokens

测试步骤：

1.userA调用模型累计达到100M;
2.再次发起调用

预期结果：

1.最后一次放行调用成功;
2.前置拒绝并返回超额提示，且不生成调用日志

***

### TC\_QUO\_004

标题：【#F6\_QUO\_限额拦截】正向：未配额用户走默认额度

优先级：P0

前置条件：

> 1.userB\@CL0100未被单独配额;
> 2.CL0100在启用租户名单内;
> 3.默认额度500M/月

测试步骤：

1.userB累计使用达到500M;
2.再次发起调用

预期结果：

1.达到默认额度后调用前置拒绝

***

### TC\_QUO\_005

标题：【#F6\_QUO\_限额豁免】正向：非启用租户用户不受限额

优先级：P1

前置条件：

> 1.userC\@CL9999不在启用租户名单;
> 2.userC本月大量调用

测试步骤：

1.userC继续发起调用

预期结果：

1.调用成功不受限额;

***

### TC\_QUO\_006

标题：【#F6\_QUO\_零额度】边界：额度为0显式禁止

优先级：P1

前置条件：

> 1.平台管理员为userD\@CL0100配置额度=0

测试步骤：

1.userD使用APIKey发起任意调用

预期结果：

1.调用被前置拒绝返回超额提示;
2.不生成调用日志

***

### TC\_QUO\_007

标题：【#F6\_QUO\_配置删除】正向：删除单独配置回归默认

优先级：P1

前置条件：

> 1.userA\@CL0100原有单独配额100M;
> 2.平台管理员删除userA的单独配额

测试步骤：

1.userA发起调用;
2.检查限额依据

预期结果：

1.userA回归走默认额度500M;
2.已消耗计数保留

***

### TC\_QUO\_008

标题：【#F6\_QUO\_累加口径】正向：只累加SUCCESS调用

优先级：P1

前置条件：

> 1.userA本月有5次SUCCESS2次FAILED1次RATE\_LIMITED

测试步骤：

1.查看userA月度用量统计;

预期结果：

1.统计只含5次SUCCESS的Token用量;
2.FAILED和RATE\_LIMITED不计入

***

### TC\_QUO\_009

标题：【#F6\_QUO\_拦截无日志】正向：限额拦截不产生日志

优先级：P1

前置条件：

> 1.userA已达额度上限

测试步骤：

1.userA发起一次调用被拦截;
2.搜索调用日志

预期结果：

1.调用返回超额错误;
2.调用日志不存在本次拦截记录

***

### TC\_QUO\_010

标题：【#F6\_QUO\_轻微超额】边界：最后一次超额不回滚

优先级：P1

前置条件：

> 1.userA额度=100M已用99.5M;
> 2.发起预估2Mtokens的调用

测试步骤：

1.调用成功返回;
2.检查累计用量

预期结果：

1.调用成功;
2.实际用量101.5M全额计入不回滚;
3.下次调用前置拒绝

***

### TC\_QUO\_011

标题：【#F6\_QUO\_多Key共享】正向：多Key共享月度额度

优先级：P1

前置条件：

> 1.userA拥有Key1消耗60M和Key2消耗30M;
> 2.userA额度=100M

测试步骤：

1.userA使用Key2发起调用

预期结果：

1.调用成功，计算统一用户key
2.两把Key共享同一用户月度限额

***

### TC\_QUO\_012

标题：【#F6\_QUO\_入口无关限额】正向：直调模型与通过接入点调用共享同一用户级月度限额

优先级：P1

前置条件：

> 1.租户CL0100在限额启用名单中
>
> 2.用户U月度Token限额为100M，已使用60M
>
> 3.用户U有API Key K1绑定了模型glm-5.1和接入点ep-public

测试步骤：

1.通过API Key K1直调模型glm-5.1，消耗10M Token

2.查看用户U月度用量（应为70M）

3.通过API Key K1调用接入点ep-public（命中glm-5.1），消耗10M Token

4.查看用户U月度用量（应为80M）

5.继续调用直到限额超额

预期结果：

1.直调模型和接入点调用的Token用量累加到同一用户级月度限额

2.两种调用入口的用量共享，不分别计算

3.限额按实际命中平台成本模型的Token用量统计，不因调用入口改变

4.累计达到100M后两种入口的调用均被限额拦截

***

### TC\_QUO\_013

标题：【#F6\_QUO\_全模型口径】正向：限额租户内已配置用户的所有大模型调用Token都计入

优先级：P1

前置条件：

> 1.租户CL0100在限额启用名单中
>
> 2.用户U已配置月度Token限额额度为100M
>
> 3.用户U可调用多个不同模型（glm-5.1、claude-sonnet等）

测试步骤：

1.用户U调用模型glm-5.1，消耗20M Token

2.用户U调用模型claude-sonnet，消耗30M Token

3.用户U通过接入点ep-public调用，消耗20M Token

4.查看用户U月度用量统计

预期结果：

1.所有大模型调用Token都计入用户U的月度限额

2.不按场景、接入点或模型类型筛选排除

3.月度用量统计为70M（20+30+20）

4.剩余30M可用于任何大模型调用

***

### TC\_QUO\_014

标题：【#F6\_QUO\_月度归零】边界：自然月归零验证

优先级：P0

前置条件：

> 1.当前7月31日23:59;
> 2.userA已达额度上限调用被拦截

测试步骤：

1.等待进入8月1日;
2.userA使用同一Key发起调用

预期结果：

1.调用成功已用Token归零;
2.额度配置沿用不变

***

### TC\_QUO\_015

标题：【#F6\_QUO\_首批启用租户】边界：首批9家启用租户名单边界验证

优先级：P0

前置条件：

> 1. 平台管理员已登录后台；
> 2. 平台限额启用租户名单已配置首批 9 家租户（CL0100/CL0200/CL0300/CL0400/CL0500/CL0600/CL0800/CL0900/CL1100）；
> 3. 存在不在名单内的租户 CL9999

测试步骤：

1. 以名单内租户 CL0100 的用户 userA 发起模型调用，累计使用接近默认额度 500M；
2. userA 达到 500M 后再次发起调用；
3. 以名单外租户 CL9999 的用户 userC 发起大量模型调用（远超 500M）；
4. 检查 userC 是否被限额拦截

预期结果：

1. userA（名单内）达到 500M 后调用被限额拦截，返回超额不可重试失败；
2. userC（名单外）不受限额约束，调用正常通过；
3. userC 的用量仍进入统计和审计

***

### TC\_QUO\_016

标题：【#F6\_QUO\_限额排除】正向：租户级Key（如indepthai）调用的用量不计入用户限额

优先级：P0

前置条件：

> 1.租户CL0100在限额启用名单中;
> 2.用户userA\@CL0100的月度Token限额为500M;
> 3.userA名下有个人级API Key KeyA;
> 4.租户CL0100存在租户级Key KeyTenant（如indepthai服务Key），创建人为系统或租户管理员

测试步骤：

1.使用KeyTenant发起多次调用，累计消耗Token 800M;
2.查询userA的用量统计和限额使用情况;
3.使用KeyA发起调用消耗Token 300M;
4.再次查询userA的限额使用情况

预期结果：

1.KeyTenant的调用量正常记录在调用日志和用量统计中;
2.KeyTenant的调用量不计入限额超出租户下限额仍可正常使用;
3.KeyA的调用量计入userA的个人限额，使用量为300M;
4.用量统计中可区分租户级Key与个人Key的用量

***

### TC\_QUO\_017

标题：【#F6\_QUO\_多端点限额】正向：多种端点调用的Token用量计入同一限额

优先级：P1

前置条件：

> 1.租户CL0100在限额启用名单中;
> 2.用户userA\@CL0100的月度Token限额为200M;
> 3.userA名下有个人级API Key KeyA;
> 4.KeyA已绑定模型glm-5.1

测试步骤：

1.使用KeyA通过/v1/chat/completions端点调用glm-5.1，消耗30M Token;
2.查看userA月度用量统计（应为30M）;
3.使用KeyA通过/v1/responses端点调用，消耗20M Token;
4.查看userA月度用量统计（应为50M）;
5.使用KeyA通过/v1/messages端点调用，消耗40M Token;
6.查看userA月度用量统计（应为90M）;
7.使用KeyA通过/v1/rerank端点调用，消耗10M Token;
8.使用KeyA通过/v1/embeddings端点调用，消耗20M Token;
9.查看userA月度用量统计（应为120M）

预期结果：

1.所有端点调用的Token用量均计入同一用户级月度限额;
2.月度用量统计正确累加所有端点的Token消耗（120M）;
3.多种端点调用方式不影响限额统计的准确性和一致性

***

### TC\_QUO\_018

标题：【#F6\_QUO\_多端点拦截】正向：达到限额后所有端点调用均被拦截

优先级：P1

前置条件：

> 1.租户CL0100在限额启用名单中;
> 2.用户userA\@CL0100的月度Token限额为100M;
> 3.userA名下有个人级API Key KeyA;
> 4.KeyA已绑定模型glm-5.1
> 5.userA已累计消耗99M Token

测试步骤：

1.使用KeyA通过/v1/chat/completions端点发起调用;
2.使用KeyA通过/v1/responses端点发起调用;
3.使用KeyA通过/v1/messages端点发起调用;
4.使用KeyA通过/v1/rerank端点发起调用;
5.使用KeyA通过/v1/embeddings端点发起调用

预期结果：

1.所有端点的调用均被限额拦截;
2.返回超额不可重试失败;
3.各端点拦截行为一致，无遗漏

***

### TC\_QUO\_019

标题：【#F6\_QUO\_删除Key不释放配额】边界：删除API Key后已消耗Token不释放

优先级：P2

前置条件：

> 1.租户CL0100在限额启用名单中;
> 2.用户userA\@CL0100的月度Token限额为100M;
> 3.userA名下有2把API Key：KeyA和KeyB;
> 4.userA已使用KeyA累计消耗60M Token

测试步骤：

1.查看userA当前月度用量统计（应为60M）;
2.删除API Key KeyA;
3.使用KeyB发起调用消耗30M Token;
4.查看userA月度用量统计（应为90M）;
5.使用KeyB继续发起调用消耗15M Token;
6.查看调用结果和用量统计

预期结果：

1.删除KeyA前userA用量为60M;
2.KeyA删除成功;
3.KeyB调用成功，Token消耗计入userA限额;
4.删除KeyA后已消耗的60M Token不释放，userA用量累计为90M;
5.第5次调用消耗10M后达到100M限额，剩余5M调用被前置拒绝;
6.删除API Key不影响用户级Token限额的累计逻辑，已用计数保留

***

### TC_QUO_020

标题：【#F6_QUO_租户级隔离】正向：同一用户在不同租户下的额度分开计算

优先级：P0

前置条件：

> 1. 用户userA同时属于租户CL0100和CL0200；
> 2. CL0100和CL0200均在限额启用名单中；
> 3. userA在CL0100的月度Token限额为100M，在CL0200的月度Token限额为200M；
> 4. userA在CL0100名下有API Key KeyCL01，在CL0200名下有API Key KeyCL02

测试步骤：

1. 使用KeyCL01（CL0100）发起模型调用，累计消耗80M Token；
2. 使用KeyCL02（CL0200）发起模型调用，累计消耗150M Token；
3. 在CL0100租户视角查看userA的用量统计；
4. 在CL0200租户视角查看userA的用量统计；
5. 使用KeyCL01（CL0100）继续发起调用，消耗30M Token；
6. 使用KeyCL02（CL0200）继续发起调用，消耗60M Token；

预期结果：

1. KeyCL01调用成功，CL0100租户下userA用量累计80M；
2. KeyCL02调用成功，CL0200租户下userA用量累计150M；
3. CL0100租户视角显示userA用量80M，剩余20M；
4. CL0200租户视角显示userA用量150M，剩余50M；
5. KeyCL01调用消耗20M后达到100M限额，剩余10M调用被前置拒绝；
6. KeyCL02调用成功消耗60M，累计达到210M后被前置拒绝；
7. CL0100租户的限额不影响CL0200租户下的调用，租户间额度完全隔离；
8. 用户在不同租户下的用量统计独立展示，互不影响

***

## F7 F7

### TC\_F7\_001

标题：【#F7\_RES\_IAM同步】正向：离职人员APIKey自动识别并删除

优先级：P0

前置条件：

> 1.租户CL0100中存在用户userX名下有2把APIKey;
>
> 2.IAM中userX状态更新为离职;
>
> 3.离职治理启用配置为开启状态

测试步骤：

1.等待定时同步任务执行;

2.进入凭证管理页面查看userX名下API Key状态;

3.使用userX名下已删除的API Key发起模型调用

预期结果：

1.userX名下的2把API Key已从凭证列表中消失;

2.使用已删除的Key调用返回鉴权失败，提示Key不存在或已删除;

***

### TC\_F7\_002

标题：【#F7\_RES\_同步失败】异常：IAM同步失败保持上次名单

优先级：P1

前置条件：

> 1.IAM接口正常工作;
> 2.已同步过一次离职名单

测试步骤：

1.模拟IAM接口超时;
2.等待下一次定时任务执行;
3.进入凭证管理页面查看离职人员名下API Key状态;
4.查看系统日志页面同步状态

预期结果：

1.无新的离职记录产生，离职人员名下API Key状态不变;
2.上次同步的离职Key状态不受影响;
3.系统日志记录同步失败信息和重试计划

***

### TC\_F7\_003

标题：【#F7\_RES\_使用中Key入库】正向：业务系统仍在使用的离职Key也进入清理流程

优先级：P0

前置条件：

> 1.租户CL0100用户userY已离职;
> 2.userY创建的API Key KeyY仍被业务系统定时调用

测试步骤：

1.IAM同步离职名单后，查询KeyY状态;
2.尝试使用KeyY调用模型

预期结果：

1.KeyY被删除;
2.使用KeyY调用被拒绝;
3.系统不因KeyY仍在使用而跳过清理

***

### TC\_F7\_004

标题：【#F7\_RES\_大量离职Key】边界：单次同步大量离职Key时批量处理性能

优先级：P1

前置条件：

> 1.租户管理员已登录
> 2.IAM同步返回50+离职人员
> 3.每个离职人员名下有1\~3把API Key

测试步骤：

1.IAM同步任务执行完成，返回大量离职人员
2.租户管理员进入离职资源清理页面
3.观察页面加载性能和列表展示
4.勾选全部离职Key执行批量删除
5.观察批量操作完成时间和状态更新

预期结果：

1.页面加载时间在可接受范围内（<3秒）
2.列表展示完整，分页或虚拟滚动可用
3.批量删除操作在可接受时间内完成
4.所有Key状态正确更新为"已删除"
5.转发侧同步逐条完成，无遗漏

***

### TC\_F7\_005

标题：【#F7\_RES\_用户ID匹配方式】正向：接口按用户ID匹配API Key创建人，不按姓名或工号

优先级：P1

前置条件：

> 1.离职人员P1：用户ID=user001，姓名=张三，工号=E001
> 2.API Key K1创建人用户ID=user001
> 3.存在另一用户P2：用户ID=user002，姓名=张三（同名），工号=E002

测试步骤：

1.IAM同步返回离职人员P1（用户ID=user001）
2.观察系统是否按用户ID匹配API Key创建人
3.验证API Key K1（创建人user001）被标记为离职资源
4.验证P2（同名但不同用户ID）的API Key不被误标记

预期结果：

1.系统按用户ID匹配API Key创建人，不按姓名或工号
2.API Key K1（创建人user001）被正确标记为离职资源
3.P2的API Key（创建人user002）不被误标记
4.同名不同用户ID的人员不会产生误匹配

***

### TC\_F7\_006

标题：【#F7\_RES\_业务接入点】正向：用户离职后，其创建的业务接入点仍正常使用

优先级：P1

前置条件：

> 1.租户CL0100用户userX已创建业务接入点ep-biz;
> 2.ep-biz已绑定多把其他用户创建的API Key;
> 3.IAM中userX状态更新为离职;
> 4.离职治理启用配置为开启状态

测试步骤：

1.等待定时同步任务执行，确认userX名下API Key被禁用;
2.使用其他用户绑定ep-biz的API Key调用模型;
3.查看接入点列表中ep-biz状态;
4.使用其他租户管理员账号登录查看ep-biz

预期结果：

1.userX名下API Key被正确禁用或删除;
2.其他用户绑定ep-biz的API Key调用正常，不受userX离职影响;
3.接入点ep-biz状态保持运行中，无变化;
4.接入点创建人仍显示为userX，但不影响接入点正常使用;
5.离职清理仅处理凭证级资源，不影响业务接入点

***

### TC\_F7\_007

标题：【#F7\_RES\_多租户Key】正向：同用户多租户下存在Key校验都删除成功

优先级：P1

前置条件：

> 1.用户userM同时存在于租户CL0100和租户CL0200中;
> 2.userM在CL0100名下有2把API Key（KeyM1、KeyM2）;
> 3.userM在CL0200名下有1把API Key（KeyM3）;
> 4.IAM中userM状态更新为离职;
> 5.离职治理启用配置为开启状态

测试步骤：

1.等待定时同步任务执行;
2.在租户CL0100中检查userM名下API Key状态;
3.在租户CL0200中检查userM名下API Key状态;
4.尝试使用KeyM1、KeyM2、KeyM3调用模型

预期结果：

1.userM在CL0100名下的KeyM1和KeyM2均被成功删除;
2.userM在CL0200名下的KeyM3被成功删除;
3.使用KeyM1、KeyM2、KeyM3调用均被拒绝;
4.同用户跨租户的所有API Key均被正确清理，无遗漏

***

## F8 F8

### TC\_SEC\_001

标题：【#F8\_SEC\_适用范围】正向：安全护栏仅适用于公共接入点

优先级：P1

前置条件：

> 1.平台管理员已登录；
> 2.存在公共接入点ep-public、业务接入点ep-biz和可直调模型M

测试步骤：

1.发布公共接入点ep-public时不勾选推荐配置；
2.查看ep-public详情；
3.进入业务接入点创建、编辑和详情页；
4.通过模型M直调发送相同高风险内容；
5.分别通过ep-public和ep-biz发送相同高风险内容

预期结果：

1.公共接入点护栏状态为未启用，无推荐配置自动套用；
2.业务接入点不展示护栏配置、状态或拦截日志入口；
3.未启用护栏的公共接入点、业务接入点和模型直调均不执行护栏检测

***

### TC\_SEC\_002

标题：【#F8\_SEC\_输入检测拦截】正向：输入命中提示词攻击高风险时阻断

优先级：P0

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.推荐配置已生效

测试步骤：

1.发送命中提示词攻击 `Unsafe` 的输入；
2.查看调用响应；
3.刷新用量统计；
4.进入公共接入点「拦截日志」Tab 查询本次请求

预期结果：

1.请求在业务模型调用前被阻断并返回安全响应；
2.安全响应包含固定文案：「很抱歉，我无法提供相关内容」；
3.拦截日志记录检测方向=输入、防护维度=提示词攻击、风险等级=高、处置动作=拦截；
4.用量统计将本次调用计入总调用数和失败调用数，不计入成功调用数；
5.未触发候选切换，未消耗业务模型Token

***

### TC_SEC_003

标题：【#F8\_SEC\_输出检测拦截】正向：输出命中内容合规高风险时阻断

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.业务模型可生成命中内容合规 `Unsafe` 的输出

测试步骤：

1.发送可触发高风险模型输出的请求；
2.查看调用响应；
3.查看用量统计；
4.在公共接入点「拦截日志」Tab 查询本次请求

预期结果：

1.输出在返回调用方前被阻断并返回安全响应；
2.安全响应包含固定文案：「很抱歉，我无法提供相关内容」；
3.用量统计将本次调用记为失败并计入失败调用数；
4.拦截日志记录方向=输出、维度=内容合规、风险等级=高和失败原因

***

### TC_SEC_004

标题：【#F8\_SEC\_推荐配置】交互：仅支持套用推荐配置且不提供自定义入口

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为未启用状态

测试步骤：

1.发布公共接入点时勾选「套用AI安全护栏推荐配置」；
2.发布后查看护栏状态和能力说明；
3.检查页面可操作项；
4.切换为拦截模式并确认

预期结果：

1.推荐配置默认以观察模式生效；
2.能力说明包含输入侧脱敏和输入/输出内容合规、提示词攻击检测；
3.页面无自定义标签、检测范围、阈值、风险映射和处置规则入口；
4.切换模式需二次确认且保存成功

***

### TC\_SEC\_005

标题：【#F8\_SEC\_输入脱敏】正向：脱敏后内容同时提供给判官模型与业务模型

优先级：P1

前置条件：

> 1.公共接入点ep-sec已套用推荐配置；
> 2.准备包含身份证号的输入

测试步骤：

1.通过ep-sec发送包含身份证号的请求；
2.查看业务模型实际接收内容和返回结果；
3.查看判官模型送检摘要

预期结果：

1.身份证号在输入侧被脱敏，调用不中断；
2.判官模型和业务模型均只接收脱敏后内容，不接收原身份证号；
3.输出侧不执行敏感内容脱敏，业务模型输出不包含脱敏占位符；
4.脱敏行为本期仅记录不阻断

***

### TC_SEC_006

标题：【#F8\_SEC\_判官检测维度】正向：内容合规和提示词攻击分别检测输入输出

优先级：P1

前置条件：

> 1.公共接入点ep-sec已套用推荐配置；
> 2.具备两个判官维度的输入和输出测试数据

测试步骤：

1.分别发送命中内容合规、提示词攻击的输入；
2.分别触发命中内容合规、提示词攻击的输出；
3.查看检测结果

预期结果：

1.内容合规与提示词攻击均覆盖输入和输出方向；
2.各结果准确记录检测方向和防护维度；
3.敏感内容不作为判官模型检测维度

***

### TC\_SEC\_007

标题：【#F8\_SEC\_多维命中】正向：同一内容命中多个判官维度时结果独立

优先级：P1

前置条件：

> 1.公共接入点ep-public已启用AI安全护栏观察模式
> 2.推荐配置已生效

测试步骤：

1.发送同时命中内容合规和提示词攻击的输入；
2.查看判官检测结果；
3.对比两个维度的标签和风险等级

预期结果：

1.两个判官维度结果独立记录且不互相覆盖；
2.每个维度的标签和风险等级准确；
3.最终处置按最高处置级别执行

***

### TC\_SEC\_008

标题：【#F8\_SEC\_观察模式】正向：观察模式命中风险不拦截调用

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为观察模式

测试步骤：

1.分别发送命中 `Unsafe` 和 `Controversial` 的内容；
2.查看调用结果和用量统计

预期结果：

1.两次调用均不因判官命中被拦截；
2.风险结果按高、中风险记录，v1不产生低风险；
3.用量统计不因观察模式命中将调用记为失败

***

### TC\_SEC\_009

标题：【#F8\_SEC\_32K上下文】边界：超长输入截断送判官但完整发送业务模型

优先级：P1

前置条件：

> 1.公共接入点ep-sec已套用推荐配置；
> 2.准备脱敏后长度分别为32K和超过32K的输入

测试步骤：

1.发送脱敏后长度为32K的输入；
2.发送脱敏后长度超过32K的输入；
3.对比判官模型与业务模型接收内容

预期结果：

1.32K输入完整送判官模型；
2.超过32K的输入按确定性规则截断至32K送判官模型且标记已截断；
3.业务模型接收完整的脱敏后输入，请求不因超长被拒绝

***

### TC\_SEC\_010

标题：【#F8\_SEC\_拦截日志】正向：公共接入点拦截日志展示完整明细

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.已产生输入和输出拦截记录

测试步骤：

1.进入公共接入点详情；
2.打开「拦截日志」Tab；
3.按request_id查询输入和输出拦截记录；
4.查看明细

预期结果：

1.Tab仅展示被拦截调用；
2.可按request_id准确定位记录；
3.明细包含调用协议、检测方向、防护维度、标签、风险等级、处置动作、失败原因和时间；
4.命中原文不直接展示

***

### TC\_SEC\_011

标题：【#F8\_SEC\_成本隔离】正向：判官模型调用不计入租户点数与用量

优先级：P1

前置条件：

> 1.租户tenant001已开通点数包；
> 2.公共接入点ep-sec已套用推荐配置并处于观察模式；
> 3.租户有API Key可调用接入点

测试步骤：

1.记录当前租户点数余额；
2.发送多次包含合规风险内容的请求；
3.查看租户调用日志和用量统计

预期结果：

1.租户调用日志中无判官模型调用记录；
2.租户用量统计中无额外token消耗；
3.租户点数余额未因判官检测而减少

***

### TC\_SEC\_012

标题：【#F8\_SEC\_模型隔离】正向：判官模型不出现在模型广场与可绑定列表

优先级：P1

前置条件：

> 1.租户管理员已登录；
> 2.判官模型已注册为平台纳管模型

测试步骤：

1.进入模型广场页面，搜索判官模型；
2.进入API Key/接入点配置页面，查看可绑定模型列表；
3.尝试通过API直调判官模型modelId

预期结果：

1.模型广场中无判官模型；
2.API Key/接入点可绑定列表中无判官模型；
3.租户直调判官模型modelId失败

***

### TC\_SEC\_013

标题：【#F8\_SEC\_策略隔离】正向：不同公共接入点推荐配置与运行模式相互独立

优先级：P1

前置条件：

> 1.公共接入点ep-A已套用推荐配置并处于拦截模式；
> 2.公共接入点ep-B安全护栏为未启用状态

测试步骤：

1.发送包含高风险内容的请求到ep-A；
2.发送相同高风险内容的请求到ep-B；
3.将ep-A切换为观察模式；
4.再次发送高风险请求到ep-A和ep-B

预期结果：

1.ep-A的高风险请求被阻断；
2.ep-B的请求正常通过，无护栏延迟；
3.ep-A运行模式修改后，ep-B仍为未启用且不受影响；
4.两个公共接入点的推荐配置和运行模式相互独立

***

### TC\_SEC\_014

标题：【#F8\_SEC\_协议兼容】正向：多种文本调用协议执行一致护栏链路

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.ep-sec支持 `v1/chat/completions` 和 `v1/responses`

测试步骤：

1.通过 `v1/chat/completions` 发送包含敏感信息和高风险提示词攻击的请求；
2.通过 `v1/responses` 发送相同内容；
3.查看两次调用结果和拦截日志

预期结果：

1.两种协议均先执行输入脱敏，再执行输入/输出判官检测；
2.两种协议命中高风险时均被拦截；
3.拦截日志准确记录对应调用协议；
4.两种协议的风险等级与处置结果一致

***

### TC\_SEC\_015

标题：【#F8\_SEC\_标签关闭】正向：关闭标签后对应内容不再命中

优先级：P1

前置条件：

> 1.接入点ep-sec安全护栏为观察模式；
> 2.内容合规维度已启用，"暴恐内容"标签已开启

测试步骤：

1.发送包含暴恐内容特征的请求；
2.确认日志中记录命中"暴恐内容"标签；
3.进入护栏配置页，关闭"暴恐内容"标签；
4.等待生效窗口（2\~5分钟）；
5.再次发送相同暴恐内容请求；
6.查看调用日志

预期结果：

1.步骤2调用日志记录命中"暴恐内容"标签；
2.步骤5调用不被阻断（观察模式）；
3.步骤6调用日志记录nonLabel=true；
4.关闭标签后对应内容不再命中检测

***

### TC\_SEC\_016

标题：【#F8\_SEC\_异步执行】正向：观察模式判官检测异步执行（低延迟）

优先级：P1

前置条件：

> 1.公共接入点ep-sec已套用推荐配置并处于观察模式；
> 2.可用压测工具测量请求延迟

测试步骤：

1.多次发送正常请求（无风险内容），记录基准延迟；
2.切换护栏模式为拦截模式；
3.多次发送相同请求，记录延迟；
4.切换回观察模式；
5.多次发送相同请求，记录延迟；
6.对比三种模式下的延迟

预期结果：

1.观察模式下延迟与基准延迟无显著差异（异步执行）；
2.拦截模式下延迟有可感知增加（同步执行）；
3.观察模式恢复后延迟回到基准水平；
4.观察模式下所有请求正常返回，检测结果正确记录

***

### TC\_SEC\_017

标题：【#F8\_SEC\_权限校验】正向：API Key权限校验失败时，安全护栏不执行检测

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.存在API Key Key1和Key2，均未授权公共接入点ep-sec；
> 3.存在已授权ep-sec的合法API Key

测试步骤：

1.使用Key1调用ep-sec；
2.查看调用日志中护栏检测结果；
3.使用Key2调用ep-sec；
4.查看调用日志中护栏检测结果；
5.使用合法API Key（绑定接入点且有权限）发送高风险请求；
6.查看调用日志中护栏检测结果

预期结果：

1.Key1调用返回权限不足错误，不触发护栏检测；
2.Key1的调用日志中无护栏检测相关记录；
3.Key2调用返回接入点权限不足错误，不触发护栏检测；
4.Key2的调用日志中无护栏检测相关记录；
5.合法API Key的高风险请求被护栏拦截；
6.合法请求的调用日志中记录护栏检测结果，权限校验在护栏检测之前执行

***

### TC\_SEC\_018


标题：【#F8\_SEC\_超时降级】异常：判官模型超时自动跳过并继续业务调用

优先级：P0

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式;
> 2.可模拟判官模型超时场景

测试步骤：

1.模拟判官模型超时；
2.发送包含身份证号和高风险内容的请求;
3.查看调用结果;
4.查看公共接入点策略状态和用量统计；
5.恢复判官模型，再次发送相同高风险请求；
6.对比两次调用结果

预期结果：

1.判官模型超时时本次检测自动跳过，请求继续调用业务模型；
2.公共接入点策略状态保持拦截模式；
3.业务模型只接收脱敏后内容，输入侧脱敏不受判官超时影响；
4.业务调用成功时不记为失败调用且不产生拦截记录；
5.判官模型恢复后高风险请求被正常拦截，无需人工切换策略

***

### TC\_SEC\_019

标题：【#F8\_SEC\_流式检测】正向：流式输出命中高风险后停止后续片段

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.目标模型支持流式调用

测试步骤：

1.发起可触发高风险输出的流式调用；
2.观察流式响应过程；
3.查看用量统计；
4.查看拦截日志

预期结果：

1.流式响应命中后停止后续片段并返回安全结束信息；
2.本次调用在用量统计中记为失败；
3.拦截日志记录输出方向、流式调用形式和风险明细

***

### TC\_SEC\_020

标题：【#F8\_SEC\_超时脱敏】异常：判官超时不影响输入侧敏感内容脱敏

优先级：P0

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.可模拟判官模型超时；
> 3.准备包含身份证号的输入

测试步骤：

1.模拟判官模型超时；
2.通过ep-sec发送包含身份证号的请求；
3.查看业务模型接收内容和调用结果；
4.查看用量统计和拦截日志

预期结果：

1.判官检测超时后自动跳过，业务调用继续；
2.业务模型只接收脱敏后内容；
3.策略状态仍为拦截模式；
4.业务调用成功时不因判官超时记为失败或拦截

***

### TC\_SEC\_021

标题：【#F8\_SEC\_直调边界】正向：业务接入点和模型直调不执行安全护栏

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.存在业务接入点ep-biz和可直调模型M

测试步骤：

1.通过ep-biz发送高风险内容；
2.通过模型M直调发送相同内容；
3.通过ep-sec发送相同内容；
4.对比调用结果

预期结果：

1.ep-biz和模型直调均不执行护栏检测；
2.ep-sec按拦截模式阻断高风险请求；
3.业务接入点无护栏配置与拦截日志入口

***

### TC\_SEC\_022

标题：【#F8\_SEC\_并发能力】边界：并发调用下判官模型容量与降级

优先级：P1

前置条件：

> 1.公共接入点ep-sec已套用推荐配置；
> 2.具备并发测试工具和目标并发基线

测试步骤：

1.按目标并发向ep-sec持续发送请求；
2.记录判官吞吐、排队、延迟和超时比例；
3.提高并发直至出现判官超时；
4.检查业务调用可用性和超时请求结果

预期结果：

1.输出目标并发下的判官吞吐、排队、延迟和超时比例；
2.判官超时请求自动跳过检测并fail-open；
3.业务调用链路保持可用，未因判官容量不足级联失败

***

### TC\_SEC\_023

标题：【#F8\_SEC\_失败统计】正向：护栏拦截在用量统计中记为失败调用

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.记录当前用量统计成功数、失败数和总调用数

测试步骤：

1.发送输入侧高风险请求并被拦截；
2.发送可触发输出侧高风险的请求并被拦截；
3.刷新用量统计；
4.进入拦截日志查看两次调用

预期结果：

1.两次拦截均计入失败调用数和总调用数，不计入成功调用数；
2.拦截日志分别记录输入、输出拦截明细；
3.用量统计不单独展示安全事件汇总

***

### TC\_SEC\_024

标题：【#F8\_SEC\_多轮诱导】正向：判官识别多轮渐进诱导

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.准备前几轮正常、后续逐步诱导违规输出的对话数据

测试步骤：

1.通过ep-sec连续发送多轮正常对话；
2.在后续轮次逐步加入诱导指令；
3.发送最终诱导请求；
4.查看调用结果和拦截日志

预期结果：

1.前几轮未达到风险条件时正常通过；
2.判官结合32K范围内多轮上下文识别渐进诱导；
3.达到高风险条件的轮次被拦截；
4.拦截日志记录提示词攻击维度和多轮上下文命中结果；
5.最终高风险轮次在用量统计中记为失败调用

***

### TC_SEC_025

标题：【#F8_SEC_误报标注】交互：拦截日志支持标记为误报

优先级：P1

前置条件：

> 1.公共接入点ep-sec安全护栏为拦截模式；
> 2.已产生被拦截的调用记录

测试步骤：

1.进入公共接入点ep-sec详情→「拦截日志」Tab；
2.定位一条拦截记录，点击「标记为误报」；
3.查看该记录状态变更；
4.尝试对已标记为误报的记录再次操作

预期结果：

1.拦截记录支持「标记为误报」操作；
2.标记后记录显示误报状态标识；
3.已标记为误报的记录不再显示「标记为误报」按钮；
4.误报标注仅改变记录显示状态，不影响历史拦截统计

***

## F12 F12

### TC\_FLT\_001

标题：【#F12\_FLT\_创建人搜索】交互：创建人按姓名/账号搜索定位用户

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录
> 2. 租户内存在用户"张三"(<zhangsan@corp.com>)

测试步骤：

1. 打开API key管理列表→创建人筛选
2. 在搜索框输入"张三"
3. 清空后输入"zhangsan"

预期结果：

1. 输入"张三"后展示匹配的用户条目（姓名+账号）
2. 输入"zhangsan"后同样展示匹配条目
3. 选择后筛选结果精确匹配该创建人的凭证

***

### TC\_FLT\_002

标题：【#F12\_FLT\_状态枚举】正向：状态筛选枚举与API Key状态模型一致

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录；
> 2. API Key状态模型定义包含：生效中、已禁用

测试步骤：

1. 打开凭证列表→状态筛选下拉；
2. 对比筛选枚举值与列表API Key状态；

预期结果：

1. 状态筛选枚举值与API Key状态模型完全对应；

***

### TC\_FLT\_003

标题：【#F12\_FLT\_普通用户状态】边界：普通用户状态筛选选项收窄

优先级：P1

前置条件：

> 1. 普通用户userA已登录；
> 2. userA名下有2把启用Key和1把已禁用Key，无其他状态Key

测试步骤：

1. 打开我的凭证列表→状态筛选下拉；
2. 查看可选状态项；

预期结果：

1. 状态筛选项仅出现userA名下实际存在的状态：生效中、已禁用；
2. 创建人筛选不展示（普通用户列表只展示自己的凭证）

***

### TC\_FLT\_004

标题：【#F12\_FLT\_组合筛选】正向：创建人与多个状态组合筛选及重置

优先级：P1

前置条件：

> 1. 租户CL0100管理员已登录；
> 2. 用户userA在CL0100中创建了启用、已禁用状态的API Key各1把；
> 3. 用户userB在CL0100中创建了启用、已禁用状态的API Key各1把；
> 4. 凭证列表数据超过一页，userA和userB的凭证分布在不同分页中

测试步骤：

1. 进入API Key管理列表；
2. 查看创建人和状态筛选项的默认值；
3. 在创建人筛选中搜索并选择userA；
4. 在状态筛选中同时选择“已禁用”；
5. 查看筛选后的凭证列表、结果总数和当前页码；
6. 清除创建人userA条件；
7. 查看仅保留“已禁用”状态后的筛选结果；
8. 点击重置全部筛选条件；
9. 查看凭证列表、结果总数和当前页码

预期结果：

1. 默认视图不预置创建人和状态条件，列表展示CL0100内全部状态的凭证；
2. 选择userA与状态后，列表只展示创建人用户ID为userA且状态属于“已禁用”的凭证；
3. 筛选后结果总数与符合条件的数据条数一致，页码自动回到第1页；
4. 清除userA后，列表展示CL0100内所有创建人的已禁用凭证，包含userA和userB的数据；
5. 重置后创建人和状态条件均为空，列表恢复全部凭证，总数恢复为筛选前数量，页码回到第1页；
6. 各次筛选过程中不展示其他租户的凭证

***

## F13 F13

### TC\_CCS\_001

标题：【#F13\_CCS\_一键填入】正向：深链拉起cc-switch导入成功

优先级：P0

前置条件：

> 1.创建人userA已登录;
> 2.测试机已安装cc-switch客户端;
> 3.userA名下有全部资源权限的APIKey

测试步骤：

1.凭证列表→userA的APIKey→点击行操作填入CCSwitch;
2.选择应用=Claude填名称test-claude选主模型=ep-public-glm;
3.生成深链;
4.点击深链或复制到浏览器打开

预期结果：

1.生成ccswitch://深链;
2.浏览器拉起cc-switch客户端provider导入成功;
3.可使用该provider调用模型

***

### TC\_CCS\_002

标题：【#F13\_CCS\_未安装】异常：未安装客户端场景处理

优先级：P1

前置条件：

> 1.创建人userA已登录;
> 2.测试机未安装cc-switch客户端

测试步骤：

1.生成深链后在浏览器打开

预期结果：

1.浏览器提示无法打开ccswitch://协议;
2.浏览器不崩溃无脚本错误

***

### TC\_CCS\_003

标题：【#F13\_CCS\_权限】正向：非创建人权限拦截

优先级：P0

前置条件：

> 1.租户管理员admin\@CL0100已登录;
> 2.用户userA有一把APIKey

测试步骤：

1.进入凭证列表→找到userA的APIKey；
2.查看行操作区域；
3.通过API工具（如Postman）直接调用明文取回接口，传入userA的API Key凭证ID；
4.验证后端接口返回结果

预期结果：

1.填入CCSwitch操作不可见或不可用；
2.admin无法对该Key执行明文取回和填入；
3.后端接口返回403状态码或权限拒绝错误信息，不执行明文取回操作；
4.审计日志中无本次明文取回操作记录

***

### TC\_CCS\_004

标题：【#F13\_CCS\_深链安全】正向：深链含明文密钥传输安全性确认

优先级：P1

前置条件：

> 1. 创建人 userA 已登录；
> 2. 测试机已安装 cc-switch 客户端；
> 3. userA 名下有全部资源权限的 API Key

测试步骤：

1. 在凭证列表点击 userA 的 API Key 行操作"填入 CC Switch"；
2. 选择应用和模型后生成深链；
3. 检查深链 URL 格式：ccswitch://v1/import?...\&apiKey=<明文密钥>；
4. 确认深链在浏览器地址栏/系统日志中是否可见明文密钥；
5. 确认深链传输是否使用 HTTPS 或本地协议安全机制

预期结果：

1. 深链 URL 中包含明文 API Key（产品口径确认：限创建人取回 + 审计日志）；
2. ccswitch:// 是本地协议，不经过网络传输（浏览器直接唤起本地应用）；
3. 每次生成深链均产生审计日志记录（操作人、时间、凭证 ID）；
4. 若深链被复制到剪贴板，剪贴板内容包含明文密钥

***

### TC\_CCS\_005

标题：【#F13\_CCS\_模型列表完整】正向：模型下拉包含 Key 权限范围内所有可调用目标（含模型直连和 ep-\* 接入点）

优先级：P1

前置条件：

> 1. 创建人 userA 已登录；
> 2. userA 名下有部分资源权限的 API Key KeyX，绑定了模型 M1 和接入点 ep-public-M；
> 3. CL0100 中还有模型 M2（已启用但 KeyX 未绑定）

测试步骤：

1. 在凭证列表点击 KeyX 行操作"填入 CC Switch"；
2. 在 CCSwitch 填入对话框中展开"主模型"下拉；3. 检查下拉列表内容

预期结果：

1. 下拉列表包含 KeyX 权限范围内的所有可调用目标： - 模型 M1（模型直连） - 接入点 ep-public-M（ep-\* 接入点）
2. 下拉列表不包含 KeyX 权限范围外的目标： - 模型 M2（KeyX 未绑定，不可调用）

***

### TC\_CCS\_006

标题：【#F13\_CCS\_禁用Key深链】异常：已禁用API Key生成深链后调用失败

优先级：P1

前置条件：

> 1.租户CL0100用户userA名下有API Key KeyD;
> 2.KeyD当前状态为"已禁用";
> 3.userA已登录平台

测试步骤：

1.userA在凭证列表找到KeyD，点击"填入CC Switch"操作生成深链;
2.复制深链并在浏览器中打开;
3.观察CC Switch导入页面行为;
4.尝试使用深链中的配置信息发起调用

预期结果：

1.已禁用KeyD可生成深链，深链URL正常生成;
2.浏览器打开深链后CC Switch导入页面正常拉起;
3.使用已禁用KeyD的配置信息调用被拒绝，返回401或提示Key已禁用;
4.深链不因Key禁用而失效，但实际调用受Key状态约束

***

### TC\_CCS\_007

标题：【#F13\_CCS\_参数过长】边界：名称过长导致深链超过2048字符

优先级：P2

前置条件：

> 1. 创建人userA已登录;
> 2. userA名下有API Key KeyX;
> 3. KeyX绑定了多个模型映射（Claude: Haiku/Sonnet/Opus）

测试步骤：

1. 在凭证列表点击KeyX行操作"填入 CC Switch";
2. 选择应用=Claude;
3. 在名称框输入超长名称（导致完整深链超过2048字符）;
4. 点击"生成并跳转";
5. 观察系统提示

预期结果：

1. 系统检测到深链超过2048字符限制;
2. 提示"参数过长，请缩短名称后重试";
3. 不生成深链，不触发跳转;
4. 用户可修改名称后重新尝试

***

### TC\_CCS\_008

标题：【#F13\_CCS\_模型映射为空】边界：未选择模型映射时深链不包含映射参数

优先级：P1

前置条件：

> 1. 创建人userA已登录;
> 2. userA名下有API Key KeyX;
> 3. KeyX绑定了主模型M1

测试步骤：

1. 在凭证列表点击KeyX行操作"填入 CC Switch";
2. 选择应用=Claude;
3. 选择主模型=M1;
4. 不选择任何模型映射（Haiku/Sonnet/Opus均不填）;
5. 点击"生成并跳转";
6. 查看生成的深链URL

预期结果：

1. 深链正常生成;
2. 深链URL中不包含模型映射参数（modelMapping）;
3. 深链包含resource、app、name、endpoint、apiKey、model参数;
4. 用户可在cc-switch中手动配置模型映射

***

### TC\_CCS\_009

标题：【#F13\_CCS\_浏览器安全策略】异常：浏览器安全策略拦截深链跳转

优先级：P1

前置条件：

> 1. 创建人userA已登录;
> 2. 测试机已安装cc-switch客户端;
> 3. 浏览器安全策略限制非HTTPS深链跳转

测试步骤：

1. 在凭证列表点击userA的API Key行操作"填入 CC Switch";
2. 选择应用和模型后生成深链;
3. 点击"生成并跳转";
4. 观察浏览器行为;
5. 查看对话框提示文案

预期结果：

1. 浏览器可能拦截非HTTPS深链跳转;
2. 对话框底部提示"如无法跳转，请检查浏览器设置或手动复制配置";
3. 系统无法绕过浏览器安全策略限制;
4. 用户可使用"复制配置"按钮手动复制配置信息

***

## F14 F14

### TC\_QRY\_001

标题：【#F14\_QRY\_多维筛选】正向：UA/IP筛选验证

优先级：P0

前置条件：

> 1.租户管理员已登录;
> 2.本月存在多种UA和IP的调用记录

测试步骤：

1.进入调用日志页面;
2.在User-Agent筛选框输入特定UA关键词筛选;
3.清除UA筛选在IP筛选框输入特定IP筛选

预期结果：

1.结果仅展示匹配该UA的调用记录;
2.结果仅展示匹配该IP的调用记录

***

### TC\_QRY\_002

标题：【#F14\_QRY\_汇总指标】交互：汇总指标区联动

优先级：P1

前置条件：

> 1.存在多天多状态的调用记录；
> 2.公共接入点ep-sec安全护栏为拦截模式；
> 3.记录当前总请求、成功、失败和限流数量

测试步骤：

1.查看调用日志列表上方汇总指标区；
2.通过ep-sec发送输入侧高风险请求并触发护栏拦截；
3.刷新调用日志页面；
4.查看总请求、成功、失败和限流数量；
5.添加筛选条件并观察指标变化

预期结果：

1.展示总请求、成功、失败、限流、平均RPM/TPM；
2.护栏拦截后总请求数和失败数各增加1，成功数和限流数不增加；
3.汇总指标区不单独展示安全事件汇总；
4.添加筛选后指标联动刷新

***

### TC\_QRY\_003

标题：【#F14\_QRY\_限流口径】正向：供应商429与限额前置拒绝分别计入限流和失败

优先级：P1

前置条件：

> 1. 平台管理员已登录用量统计页面；
> 2. 准备可触发供应商429响应的API Key Key429；
> 3. 用户userA的月度Token用量已达到限额，准备其API Key KeyQuota；
> 4. 记录当前汇总指标区的总请求、成功、失败和限流数量

测试步骤：

1. 使用Key429发起调用并触发供应商429响应；
2. 记录该调用的request_id；
3. 使用KeyQuota发起一次模型调用；
4. 记录限额拒绝响应中的错误码；
5. 刷新用量统计页面；
6. 查看汇总指标区的失败和限流数量；
7. 在调用日志中按步骤2的request_id搜索429调用；
8. 在调用日志中按KeyQuota及步骤3的调用时间范围搜索限额拒绝请求

预期结果：

1. Key429调用返回HTTP 429或等价供应商限流错误；
2. KeyQuota调用在模型执行前返回超额不可重试错误；
3. 汇总指标中的限流数量相对基线增加1，429调用不重复计入失败数量；
4. 汇总指标中的失败数量相对基线增加1，限额拒绝不计入限流数量；
5. 调用日志中可按request_id查询到429调用记录，状态或错误信息显示供应商限流；
6. 调用日志中不存在步骤3的限额前置拒绝记录；
7. 限额拒绝通过接口错误响应和汇总失败指标可验证，不依赖调用日志明细

***

### TC\_QRY\_004

标题：【#F14\_QRY\_模型TOP5联合展示】正向：模型 TOP5 卡片以 modelId + providerName 联合展示，同名不同供应商分开

优先级：P0

前置条件：

> 1. 平台管理员已登录用量统计页面；
> 2. 近期存在通过公共接入点分别命中 ali+glm-5.1 和 qwen+glm-5.1 的调用记录；
> 3. 两者的调用次数均进入 TOP 5

测试步骤：

1. 查看模型 TOP 5 卡片；
2. 检查 glm-5.1 在卡片中的展示方式；
3. 点击 ali+glm-5.1 行，观察跳转到明细后的筛选条件；
4. 点击 qwen+glm-5.1 行，观察跳转到明细后的筛选条件

预期结果：

1. 模型 TOP 5 卡片中 glm-5.1 分两行展示： - 行1：glm-5.1（ali） - 行2：glm-5.1（qwen）
2. 每行展示 modelId + providerName 联合标识；
3. 点击 ali 行跳转明细后，筛选条件自动设置为实际命中模型=ali+glm-5.1；
4. 点击 qwen 行跳转明细后，筛选条件自动设置为实际命中模型=qwen+glm-5.1

***

### TC\_QRY\_005

标题：【#F14\_QRY\_APIKeyTOP5用户列】正向：API Key TOP 5 卡片展示用户列（创建人）

优先级：P1

前置条件：

> 1.平台管理员已登录用量统计页面;
> 2.API Key TOP 5 卡片有数据

测试步骤：

1.查看API Key TOP 5卡片;
2.检查"API 密钥"与"请求数"之间是否有"用户"列;
3.确认用户列显示的是API Key创建人信息;
4.对比API Key管理页面中的创建人信息

预期结果：

1.API Key TOP 5卡片中"API 密钥"与"请求数"之间展示"用户"列;
2.用户列显示API Key创建人姓名或用户ID;
3.用户列信息与API Key管理页面中的创建人信息一致;
4.创建人为空的API Key显示"-"或"无"

***

### TC\_QRY\_006

标题：【#F14\_QRY\_视角筛选约束】正向：各角色筛选项受用量视角约束

优先级：P1

前置条件：

> 1. 分别准备普通用户userA、租户管理员admin\_T、平台管理员admin\_P账号

测试步骤：

1. 以userA登录→用量统计页面
2. 以admin\_T登录→用量统计页面
3. 以admin\_P登录→用量统计页面

预期结果：

1. userA页面：无"用户"筛选项，只可在"我的用量"范围内筛选
2. admin\_T页面："用户"筛选项可选，租户维度走视角切换非独立筛选器
3. admin\_P页面：租户维度走视角切换下拉，不重复提供租户筛选器

***

### TC\_QRY\_007

标题：【#F14\_QRY\_同名模型筛选分开】正向：按实际命中模型筛选时同名不同供应商分开呈现

优先级：P1

前置条件：

> 1. 模型glm-5.1来自ali和qwen两个供应商
> 2. 近期有通过公共接入点分别命中两者的调用记录

测试步骤：

1. 在用量统计明细页面，展开"实际命中模型"筛选下拉
2. 查看glm-5.1的筛选项

预期结果：

1. glm-5.1在筛选下拉中按供应商分开呈现：glm-5.1(ali)和glm-5.1(qwen)
2. 选择glm-5.1(ali)后结果仅展示命中ali渠道的调用记录

***

### TC\_QRY\_008

标题：【#F14\_QRY\_供应商无筛选器】正向：供应商来源仅作展示列不提供独立筛选器

优先级：P1

前置条件：

> 1. 平台管理员已登录用量统计页面

测试步骤：

1. 查看用量统计明细表的列设置
2. 查看筛选条件区域

预期结果：

1. 明细表中供应商来源列存在且可展示
2. 筛选条件区域无"供应商来源"独立筛选器
3. 供应商来源可用于聚合分组展示

***

### TC\_QRY\_009

标题：【#F14\_QRY\_卡片跳转筛选】交互：TOP5卡片行点击跳转明细并自动应用筛选

优先级：P2

前置条件：

> 1. 平台管理员已登录用量统计页面
> 2. API Key TOP5卡片存在数据

测试步骤：

1. 点击API Key TOP5卡片第一行（Key=KeyA, 用户=userA）
2. 查看下方明细列表

预期结果：

1. 明细列表自动展开并应用筛选：API Key=KeyA
2. 如卡片有对应"用户"列，用户筛选同步应用为userA
3. 当前视图的时间范围随同应用为明细时间筛选项

***

### TC\_QRY\_010

标题：【#F14\_QRY\_缓存Token展示】正向：缓存读写Token列正确展示

优先级：P1

前置条件：

> 1. 平台管理员已登录用量统计页面；
> 2. 调用日志中存在含缓存读写Token的记录

测试步骤：

1. 进入调用日志明细页；
2. 查看列表中的"缓存读取Token"和"缓存写入Token"列；
3. 点击某条记录查看详情；
4. 验证缓存Token值是否正确

预期结果：

1. 列表中展示"缓存读取Token"和"缓存写入Token"列；
2. 缓存命中的记录显示非零的缓存读取Token值；
3. 缓存写入的记录显示非零的缓存写入Token值；
4. 详情页中缓存Token值与列表一致；
5. 无缓存操作的记录显示0或"-"

***

### TC\_QRY\_011

标题：【#F14\_QRY\_调用形式】正向：调用形式列展示流式/非流式

优先级：P1

前置条件：

> 1. 平台管理员已登录用量统计页面；
> 2. 调用日志中存在流式和非流式调用记录

测试步骤：

1. 进入调用日志明细页；
2. 查看列表中的"调用形式"列；
3. 筛选出流式调用记录；
4. 筛选出非流式调用记录；
5. 查看非流式调用记录的TTFT列

预期结果：

1. 调用形式列正常展示；
2. 流式调用显示"流式"；
3. 非流式调用显示"非流式"；
4. 筛选功能正常，可按调用形式过滤；
5. 非流式调用的TTFT列显示为"—"

***

### TC\_QRY\_012

标题：【#F14\_QRY\_钻展独立时间筛选】正向：TOP 5卡片二级钻展后明细页独立调整时间筛选

优先级：P2

前置条件：

> 1.用户已登录用量统计页面;
> 2.TOP 5 API Key卡片有数据

测试步骤：

1.点击TOP 5 API Key卡片中某一行，跳转到明细列表;
2.验证明细列表自动应用了当前视图的时间范围;
3.在明细页独立调整时间范围（如从7天改为30天）;
4.返回用量统计页面，验证上层视图的时间范围不被影响

预期结果：

1.钻展时明细列表携带当前时间范围;
2.明细页可独立调整时间筛查;
3.返回上层后原始时间范围不受影响

***

### TC\_QRY\_013

标题：【#F14\_QRY\_离职删除Key记录保留】正向：离职人员已删除的API Key用量统计调用记录仍可查询

优先级：P0

前置条件：

> 1.用户userX已离职，其名下的API Key KeyX已被自动删除;
> 2.KeyX在被删除前有多条调用记录（包含成功、失败、不同模型调用）;
> 3.平台管理员已登录

测试步骤：

1.进入用量统计页面;
2.在API Key筛选框搜索KeyX（已删除的Key）;
3.查看用量统计结果是否展示KeyX的历史调用记录;
4.进入调用日志页面;
5.在API Key筛选框搜索KeyX;
6.查看调用日志是否展示KeyX的历史调用明细;
7.点击某条历史调用记录查看详情;
8.验证详情页数据完整性

预期结果：

1.用量统计页面可查询到KeyX的历史调用记录;
2.调用日志页面可查询到KeyX的历史调用明细;
3.调用记录完整展示（包含request\_id、modelId、providerId、status、tokens\_total、latency等）;
4.详情页数据完整，不因API Key被删除而丢失;
5.离职人员API Key删除不影响历史调用数据的可查询性;
6.数据保留期限符合平台数据保留策略

***

## F15 F15

### TC\_VTX\_001

标题：【#F15\_VTX\_代理】正向：代理出网验证

优先级：P1

前置条件：

> 1.已创建vertex供应商;
> 2.配置了代理地址

测试步骤：

1.纳管模型;
2.使用该供应商调用;
3.检查出口IP

预期结果：

1.调用成功;
2.出口IP为代理服务器IP

***

### TC\_VTX\_002

标题：【#F15\_VTX\_连通性】正向：连通性测试可用

优先级：P0

前置条件：

> 1.已创建vertex供应商;
> 2.SAJSON凭证有效

测试步骤：

1.进入供应商详情→点击连通性测试

预期结果：

1.测试结果展示连通成功

***

### TC\_VTX\_003

标题：【#F15\_VTX\_非目标模型】边界：创建Vertex供应商时不为其他Vertex模型做额外适配

优先级：P2

前置条件：

> 1. 平台管理员已登录

测试步骤：

1. 创建vertex类型供应商，凭证配置正确
2. 在模型列表中尝试纳管非Gemini/Claude的Vertex模型（如Imagen）

预期结果：

1. Gemini和Claude模型可正常纳管和调用
2. 其他Vertex模型创建映射、证调用适配正常

***

### TC\_VTX\_004

标题：【#F15\_VTX\_SA不回显】正向：SA JSON 保存后编辑页面字段掩码验证

优先级：P2

前置条件：

> 1. 平台管理员已登录；
> 2. 已创建 vertex 类型供应商 vtx-supplier，SA JSON 已保存

测试步骤：

1. 进入模型供应商管理 → 选择 vtx-supplier → 点击"编辑"；
2. 查看 SA JSON 字段的展示状态；
3. 检查页面源码/网络请求中是否返回 SA JSON 原文

预期结果：

1. 编辑页面 SA JSON 字段显示掩码；
2. 前端网络请求（API 返回值）中不包含 SA JSON 原文；
3. 保存时若未修改 SA JSON 字段，原值保持不变（不因空值而清除）

***

### TC\_VTX\_005

标题：【#F15\_VTX\_无代理直连】异常：无代理地址配置场景下直连出网验证

优先级：P1

前置条件：

> 1. 平台管理员已登录；
> 2. 已创建 vertex 类型供应商 vtx-direct，未配置代理地址；
> 3. SA JSON 凭证有效

测试步骤：

1. 进入 vtx-direct 供应商详情 → 点击"连通性测试"；
2. 连通性测试；
3. 纳管 Gemini 模型；
4. 使用该供应商发起模型调用；

预期结果：

1. 连通性测试失败；
2. 模型调用失败。

***

### TC\_VTX\_006

标题：【#F15\_VTX\_SA格式错误】异常：Service Account JSON格式错误/无效时的校验拒绝

优先级：P1

前置条件：

> 1.平台管理员已登录
> 2.准备创建Vertex类型供应商

测试步骤：

1.场景A：SA JSON为空字符串
2.场景B：SA JSON为非JSON格式文本（如纯文本、HTML）
3.场景C：SA JSON为合法JSON但缺少必填字段（如缺少private\_key）
4.场景D：SA JSON为合法JSON但字段值无效（如client\_email为空）
5.观察每种场景的创建行为

预期结果：

1.场景A：创建失败，提示"Service Account JSON不能为空"
2.场景B：创建失败，提示"JSON格式无效"
3.场景C：创建失败，提示缺少必填字段
4.场景D：创建失败，提示字段值无效
5.所有格式错误在提交时前端校验拦截，不发送到后端

***

### TC\_VTX\_007

标题：【#F15\_VTX\_Region无效】异常：无效Region配置时连通性测试失败行为

优先级：P1

前置条件：

> 1.平台管理员已登录
> 2.已创建Vertex供应商，SA JSON和Project ID正确
> 3.Region配置为无效值（如"invalid-region"）

测试步骤：

1.点击连通性测试按钮
2.观察测试结果
3.查看错误提示内容
4.修改Region为有效值后重新测试

预期结果：

1.连通性测试失败，返回明确错误提示
2.错误提示包含Region无效信息
3.不因Region无效导致页面异常或崩溃
4.修改为有效Region后连通性测试成功

***

### TC\_VTX\_008

标题：【#F15\_VTX\_供应商状态流转】正向：供应商状态流转（已启用/已禁用）

优先级：P1

前置条件：

> 1.已创建vertex供应商;
> 2.供应商状态为"已启用"

测试步骤：

1.进入供应商详情，点击"禁用";
2.查看供应商状态变为"已禁用";
3.尝试使用该供应商调用模型;
4.点击"启用"，查看状态恢复为"已启用";
5.再次调用模型验证

预期结果：

1.供应商状态从"已启用"变为"已禁用";
2.禁用状态下调用失败，提示供应商已禁用;
3.启用后状态恢复为"已启用";
4.调用成功正常返回

***

### TC\_VTX\_009

标题：【#F15\_VTX\_模型连通性状态流转】正向：模型连通性状态流转（未测试/测试中/成功/失败）

优先级：P1

前置条件：

> 1.已创建vertex供应商;
> 2.供应商下已纳管Gemini模型

测试步骤：

1.查看新建模型后的连通性状态（应为"未测试"）;
2.点击"测试连通性"，查看状态变为"测试中";
3.等待测试完成，验证状态变为"成功"或"失败";
4.若失败，修改配置后重新测试

预期结果：

1.新建模型后连通性状态为"未测试";
2.点击测试后状态变为"测试中";
3.网关校验成功→状态"成功";
4.网关校验失败→状态"失败"，展示失败原因

***

### TC\_VTX\_010

标题：【#F15\_VTX\_基础创建】正向：创建vertex类型模型供应商并纳管Gemini/Claude模型

优先级：P2

前置条件：

> 1.平台管理员已登录

测试步骤：

1.进入模型供应商管理→点击新建;
2.供应商类型选择vertex;
3.填写Service Account JSON（多行粘贴）、Region、Project ID;
4.保存;
5.在该供应商下纳管Vertex上的Gemini和Claude模型

预期结果：

1.创建成功;
2.SA JSON保存后不回显;
3.模型纳管成功，可正常列表展示

***

### TC\_VTX\_011

标题：【#F15\_VTX\_代理地址边界】异常：非法和回环代理地址不导致循环请求

优先级：P1

前置条件：

> 1. 平台管理员已登录；
> 2. 已进入新建Vertex供应商页面；
> 3. 已填写有效的Service Account JSON、Region和Project ID；
> 4. 平台访问地址为https://ai-hub-test.digiwincloud.com.cn/；
> 5. 准备有效代理地址http://proxy.example.com:8080和2050字符的超长URL

测试步骤：

1. 在代理地址中输入https://ai-hub-test.digiwincloud.com.cn/并点击保存；
2. 观察页面响应、按钮状态和网络请求；
3. 将代理地址改为http://127.0.0.1:8080并点击保存；
4. 观察页面响应、按钮状态和网络请求；
5. 将代理地址改为proxy.example.com:8080并点击保存；
6. 将代理地址改为http://proxy.example.com:99999并点击保存；
7. 将代理地址改为准备好的2050字符URL并点击保存；
8. 将代理地址修正为http://proxy.example.com:8080；
9. 再次点击保存；
10. 进入已保存供应商详情并点击连通性测试；
11. 查看连通性测试结果

预期结果：

1. 平台自身地址和127.0.0.1回环地址不会引发页面循环请求、持续跳转、重复提交或浏览器卡死；
2. 系统在前端拒绝危险地址，或由后端在限定响应时间内返回明确失败结果；保存按钮在响应后恢复可点击状态；
3. 缺少协议的地址被拒绝并提示需要填写完整的HTTP或HTTPS URL；
4. 端口99999被拒绝并提示端口不在有效范围；
5. 2050字符URL被拒绝并提示超过代理地址长度限制；
6. 每个非法地址最多产生一次保存请求，不创建Vertex供应商记录；
7. 首次校验失败后修正为有效地址，错误状态被清除，保存按钮可再次点击；
8. 有效代理地址保存成功，详情页展示已配置代理但不暴露敏感凭证；
9. 连通性测试在限定响应时间内结束，并展示网关返回的成功或明确失败结论；
10. 全过程页面无白屏、脚本报错或无限加载

***

## F16 F16

### TC\_I18N\_001

标题：【#F16\_I18N\_英文】正向：英文错误文案验证

优先级：P0

前置条件：

> 1.拥有有效APIKey;
> 2.知道网关错误码

测试步骤：

1.发送Accept-Language=en触发鉴权失败;
2.检查响应message字段

预期结果：

1.message返回英文文案;
2.error\_code和http\_status与中文请求一致

***

### TC\_I18N\_002

标题：【#F16\_I18N\_默认中文】正向：未携带Accept-Language或语言无法识别时返回简体中文

优先级：P1

前置条件：

> 1.拥有有效APIKey

测试步骤：

1.发送不带Accept-Language头的请求触发网关错误;
2.发送Accept-Language=xxx（不存在的语言）触发网关错误;
3.发送Accept-Language为空触发网关错误;
4.检查每次响应的message字段

预期结果：

1.步骤1返回简体中文文案;
2.步骤2返回简体中文文案;
3.步骤3返回简体中文文案;
4.所有响应的error\_code和http\_status一致

***

### TC\_I18N\_003

标题：【#F16\_I18N\_繁中】正向：繁中文案验证

优先级：P1

前置条件：

> 1.同TC\_I18N\_001

测试步骤：

1.发送Accept-Language=zh-TW触发鉴权失败

预期结果：

1.message返回繁体中文文案

***

### TC\_I18N\_004

标题：【#F16\_I18N\_透传】正向：上游错误透传验证

优先级：P1

前置条件：

> 1.模型供应商返回原始错误

测试步骤：

1.触发供应商返回原始错误;
2.分别用Accept-Language=en和zh-CN发送请求

预期结果：

1.上游原始错误信息按原样返回不被翻译;
2.两种语言请求返回的上游错误信息相同

***

### TC\_I18N\_005

标题：【#F16\_I18N\_结构化】正向：错误码字段跨语言一致性

优先级：P0

前置条件：

> 1.同TC\_I18N\_001

测试步骤：

1.分别用zh-CN/zh-TW/en触发同一网关错误码;
2.对比三次响应结构

预期结果：

1.error\_code值完全一致;
2.http\_status值完全一致;
3.结构化error.type字段完全一致;
4.仅message文案随语言变化

***

### TC\_I18N\_006

标题：【#F16\_I18N\_日志语言标记】正向：调用日志记录网关返回的error\_code与匹配到的i18n语言

优先级：P1

前置条件：

> 1. 存在可触发网关错误的请求场景（如鉴权失败）

测试步骤：

1. 分别发送 Accept-Language=en 和 Accept-Language=zh-TW 的错误请求
2. 查询对应的调用日志记录

预期结果：

1. Accept-Language=en 的调用日志记录 error\_code 和 i18n\_language=en
2. Accept-Language=zh-TW 的调用日志记录 error\_code 和 i18n\_language=zh-TW
3. 日志中 error\_code 和语言标记可被检索和过滤

***

### TC\_I18N\_007

标题：【#F16\_I18N\_不支持语言回退】边界：Accept-Language传入不支持的语言时回退简中

优先级：P1

前置条件：

> 1.存在可触发网关错误的请求场景

测试步骤：

1.场景A：Accept-Language=ja（日语，不在支持范围）
2.场景B：Accept-Language=ar（阿拉伯语，不在支持范围）
3.场景C：Accept-Language=fr-FR（法语，不在支持范围）
4.场景D：Accept-Language=xx-YY（完全虚构语言标签）
5.观察每种场景的错误响应文案语言

预期结果：

1.所有不支持语言场景均回退为简体中文（zh-CN）文案
2.错误码、HTTP状态码、结构化error.type字段保持一致
3.仅展示文案随回退语言变化
4.调用日志记录i18n\_language=zh-CN（回退）

***

### TC\_I18N\_008

标题：【#F16\_I18N\_未映射错误码透传】正向：未落到i18n映射表的网关错误码保留原始错误上下文透传

优先级：P1

前置条件：

> 1.存在可触发网关返回未映射错误码的请求场景
> 2.该错误码不在i18n三语言文案映射表中

测试步骤：

1.触发返回未映射错误码的网关错误
2.观察错误响应内容
3.验证响应不返回空字符串或"未知错误"占位
4.验证原始错误上下文被保留透传
5.查看调用日志中的error\_code记录

预期结果：

1.未映射错误码保留原始错误信息透传返回
2.不返回空字符串或"未知错误"占位文案
3.错误响应包含完整的上游错误上下文
4.error\_code字段值正确记录到调用日志
5.调用日志记录i18n\_language为实际匹配语言或"未映射"

***

### TC\_I18N\_009

标题：【#F16\_I18N\_语言权重】正向：按Accept-Language权重匹配语言

优先级：P1

前置条件：

> 1.存在可触发网关错误的请求场景（如鉴权失败）

测试步骤：

1.发送Accept-Language=en-US,en;q=0.9,zh-CN;q=0.8触发网关错误；
2.发送Accept-Language=zh-CN,en;q=0.9触发网关错误；
3.发送Accept-Language=en;q=0.5,zh-CN;q=0.8触发网关错误；
4.发送Accept-Language=zh-TW,zh-CN;q=0.5触发网关错误；
5.检查每次响应的message字段语言

预期结果：

1.步骤1返回英文文案（权重最高）；
2.步骤2返回简体中文文案（权重最高）；
3.步骤3返回简体中文文案（权重0.8 > 0.5）；
4.步骤4返回繁体中文文案（权重最高）；
5.所有响应的error\_code和http\_status保持一致

***

### TC\_I18N\_010

标题：【#F16\_I18N\_文档一致性】正向：mop\_docs错误码文档与网关运行时映射一致性验证

优先级：P2

前置条件：

> 1.获取mop\_docs仓库最新三语言错误码文档;
> 2.获取网关运行时错误码映射表

测试步骤：

1.对比mop\_docs中每个错误码的英文、简中、繁中文案与网关映射表对应条目;
2.检查是否有网关已有但文档缺失的错误码;
3.检查是否有文档已有但网关未映射的错误码

预期结果：

1.所有网关映射的错误码在mop\_docs中均有对应的三语言文档条目;
2.不存在文档缺失的错误码;
3.不存在文档中已定义但网关未映射的错误码

***

### TC\_I18N\_011

标题：【#F16\_I18N\_非UTF8透传】正向：上游错误含非UTF-8字符时的透传完整性

优先级：P2

前置条件：

> 1.模拟供应商上游返回包含非UTF-8字符（如Latin-1编码）的错误响应

测试步骤：

1.发起调用触发上游返回含非UTF-8字符的错误;
2.检查调用方接收到的错误响应内容

预期结果：

1.错误信息按透传原则返回，不被翻译;
2.非UTF-8字符不导致网关崩溃或响应截断;
3.调用日志记录完整的上游错误上下文

***

### TC\_I18N\_012

标题：【#F16\_I18N\_页面多语言交互】正向：切换繁体/英语/简体中文后页面交互正常

优先级：P1

前置条件：

> 1. 用户已登录平台；
> 2. 平台支持 zh-CN（简体）、zh-TW（繁体）、en（英语）三种语言

测试步骤：

1. 切换语言为繁体中文（zh-TW）；
2. 浏览新增页面与各页面（模型广场、接入点、用量统计、调用日志等），验证页面展示与交互正常；
3. 切换语言为英语（en）；
4. 再次浏览各页面，验证页面展示与交互正常；
5. 切换语言为简体中文（zh-CN）；
6. 验证页面恢复简体中文展示与交互

预期结果：

1. 繁体中文模式下，页面文案、菜单、按钮、提示信息均正确显示为繁体中文，布局无错乱；
2. 英语模式下，页面文案、菜单、按钮、提示信息均正确显示为英语，布局无错乱；
3. 简体中文模式下，页面文案、菜单、按钮、提示信息均正确显示为简体中文，布局无错乱；
4. 三种语言切换过程中，页面交互功能（搜索、筛选、分页、弹窗、抽屉等）均正常工作；
5. 无残留翻译、硬编码中文/英文、乱码或布局异常

***

### TC_I18N_013

标题：【#F16_I18N_浏览器兼容】正向：主流浏览器兼容性验证

优先级：P1

前置条件：

> 1. 准备Chrome（最新版）、Edge（最新版）、Firefox（最新版）、Safari（最新版）浏览器；
> 2. 用户已登录平台；
> 3. 平台支持 zh-CN（简体）、zh-TW（繁体）、en（英语）三种语言

测试步骤：

1. 使用Chrome浏览器登录平台，切换语言为繁体中文，浏览各页面验证展示与交互正常；
2. 使用Chrome浏览器切换语言为英语，验证展示与交互正常；
3. 使用Edge浏览器重复步骤1-2；
4. 使用Firefox浏览器重复步骤1-2；
5. 使用Safari浏览器重复步骤1-2；
6. 在各浏览器中验证CC Switch深链跳转功能正常；
7. 在各浏览器中验证表格排序、筛选、分页功能正常；
8. 在各浏览器中验证弹窗、抽屉、表单输入等交互功能正常

预期结果：

1. 所有主流浏览器中，页面文案、菜单、按钮、提示信息均正确显示，布局无错乱；
2. 三种语言切换功能在所有浏览器中均正常工作；
3. CC Switch深链跳转在所有浏览器中均正常（Safari可能需要手动允许打开）；
4. 表格排序、筛选、分页功能在所有浏览器中均正常；
5. 弹窗、抽屉、表单输入等交互功能在所有浏览器中均正常；
6. 无浏览器特有的样式错乱、脚本错误或功能异常
