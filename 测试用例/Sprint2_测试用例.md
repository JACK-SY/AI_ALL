# Sprint2 测试用例（模型平台）

> 来源：Sprint2_测试用例.xlsx | Sheet：用例 | 总用例数：52 | 优先级：1/2/3→P1/P2/P3 | 编号：Sprint3保留原值，Sprint1/2生成行号编号

---

## 模块概览

| 模块 | 模块名称 | 用例数 | P1 | P2 | P3 |
| ------ | ------ | ------ | ------ | ------ | ------ |
| 60957 | 【S2需求迭代】接入点管理模块 | 52 | 2 | 40 | 10 |
| **合计** |  | **52** | **2** | **40** | **10** |

---

## 60957 【S2需求迭代】接入点管理模块

### TC_S2_001

**标题**：【#60957_接入点_功能范围】（功能点 01）正常场景：接入点增删改查启停全流程可用

**优先级**：P1

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已登录普通租户账号；
> 2. 拥有接入点管理权限；
> 3. 存在已纳管模型。

**测试步骤**：

1. 进入接入点列表，点击创建；
2. 填写合法字段并创建；
3. 列表查看该接入点；
4. 编辑名称 / 描述；
5. 执行停止；
6. 执行启用；
7. 执行删除。

**预期结果**：

1. 正常跳转创建页；
2. 创建成功并展示；
3. 信息完整；
4. 修改生效；
5. 状态变为停止；
6. 状态恢复正常；
7. 删除成功。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_002

**标题**：【#60957_接入点_功能范围】（功能点 02）正常场景：接入点与 API Key 绑定解绑正常

**优先级**：P1

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已创建正常接入点；
> 2. 已创建可用 API Key。

**测试步骤**：

1. 进入 API Key 编辑页；
2. 绑定目标接入点；
3. 接入点详情查看已授权 Key，CURL调用；
4. 执行解绑，CURL调用。

**预期结果**：

1. 进入绑定界面；
2. 绑定成功；
3. 反向展示正常，CURL调用成功；
4. 授权立即失效，CURL调用失败。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_003

**标题**：【#60957_接入点_列表页】（功能点 01）正常场景：列表表格列信息完整

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 存在接入点数据；
> 2. 已登录。

**测试步骤**：

1. 进入接入点列表。

**预期结果**：

1. 列信息完整：名称 + ID、描述、应用、负责人、模型、来源、状态、创建人、ID、操作；
2. ID 可复制，名称可跳转详情；
3. 状态展示正常绿、停用灰。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_004

**标题**：【#60957_接入点_列表页】（功能点 02）正常场景：搜索筛选功能正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 存在多状态、多名称接入点；
> 2. 已登录。

**测试步骤**：

1. 搜索名称 / ID ；
2. 状态筛选切换；
3. 公共 / 业务 Tab 切换；
4. 搜索结果上下翻页。

**预期结果**：

1. 模糊匹配准确，不存在的搜索为空；
2. 结果与状态一致；
3. 展示对应类型；
4. 上下翻页展示数据准确。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_005

**标题**：【#60957_接入点_创建页】（功能点 01）正常场景：公共接入点仅超管可创建

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 登录超管；
> 2. 登录普通租户。

**测试步骤**：

1. 超管创建公共接入点；
2. 普通账号尝试创建。

**预期结果**：

1. 创建成功，全局可见；
2. 无入口或提示无权限。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_006

**标题**：【#60957_接入点_创建页】（功能点 02）正常场景：接入点 ID 字段校验正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入创建页；
> 2. 存在已占用 ID。

**测试步骤**：

1. 查看 ep - 前缀；
2. 输入合法后缀；
3. 输入非法（非英文小写 + 数字 +‘-’） / 超长字符（>64）/超短（<6）；
4. 输入已存在 ID。

**预期结果**：

1. 前缀不可删；
2. 校验通过；
3. 拦截提示错误，超长时输入不了；
4. 实时红字冲突。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_007

**标题**：【#60957_接入点_创建页】（功能点 03）正常场景：创建页必填项校验正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入创建页。

**测试步骤**：

1. ID空字段提交；
2. 接入点名称空字段提交；
3. 用途描述空字段提交；
4. 所属应用空字段提交;
5. 业务负责人空字段提交;
6. 不选择模型；

**预期结果**：

1. 提示必填；
2. 提示必填；
3. 提示必填；
4. 提示必填；
5. 提示必填；
6. 提示至少选择一个。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_008

**标题**：【#60957_接入点_创建页】（功能点 04）正常场景：页面字段其它校验正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入创建页。

**测试步骤**：

1. 接入点名称输入合法；
2. 接入点名称输入非法（非中英文 / 数字 / 空格） / 超长字符（>50）；
3. 用途描述输入超长字符（>300）;
4. 所属应用、业务负责人超长字符（>50）。

**预期结果**：

1. 校验通过；
2. 超长输入不了；
3. 超长输入不了；
4. 超长输入不了。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_009

**标题**：【#60957_接入点_创建页】（功能点 05）正常场景：模型选择弹窗功能正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入创建页，点击添加模型。

**测试步骤**：

1. 切换供应商 Tab；
2. 切换广场 / 仓库；
3. 查看无模型提示；
4. 搜索指定模型；
5. 选择并确定。

**预期结果**：

1. 可切换过滤；
2. 展示对应模型；
3. 展示缺省图；
4. 指定模型搜索展示成功；
5. 正常回填。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_010

**标题**：【#60957_接入点_删除】（功能点 01）正常场景：无绑定 Key 删除接入点

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 停止状态、无绑定 Key 接入点；
> 2. 登录创建人。

**测试步骤**：

1. 点击删除；
2. 查看弹窗；
3. 确认删除。

**预期结果**：

1. 二次确认；
2. 提示无绑定 Key；
3. 删除成功。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_011

**标题**：【#60957_接入点_删除】（功能点 02）正常场景：有绑定 Key 删除接入点

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 停止接入点已绑定 N 个 Key；
> 2. 有权限账号。

**测试步骤**：

1. 点击删除；
2. 查看风险提示；
3. 确认删除。

**预期结果**：

1. 二次确认；
2. 提示绑定数量与风险；
3. 删除成功，授权失效。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_012

**标题**：【#60957_接入点_详情页】（功能点 01）正常场景：顶部信息条正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入详情页。

**测试步骤**：

1. 查看名称、状态；
2. 查看启停按钮。

**预期结果**：

1. 信息正确；
2. 正常→停止，停止→启用。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_013

**标题**：【#60957_接入点_详情页】（功能点 02）正常场景：概览信息 Tab 展示正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入概览 Tab；
> 2. 已绑定 Key。

**测试步骤**：

1. 查看基本信息；
2. 查看计费并点击账单明细；
3. 查看绑定模型，切换模型；
4. 查看 Key 列表。

**预期结果**：

1. 信息正确，接入点ID可复制；
2. 跳转用量统计，自动带入筛选条件搜索；
3. 模型 / 来源 / 协议正确，模型切换成功；
4. 普通用户不可查看，管理员可查看，绑定的key信息展示正确。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_014

**标题**：【#60957_接入点_详情页】（功能点 03）正常场景：绑定key数量多时展示正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入概览 Tab；
> 2. 已绑定大于10个 Key。

**测试步骤**：

1.  查看 Key 列表。

**预期结果**：

1. key列表可以上下滚动查看。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_015

**标题**：【#60957_接入点_详情页】（功能点 04）正常场景：API 接入 Tab 功能正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入 API 接入 Tab。

**测试步骤**：

1. 创建 / 选择 Key 绑定；
2. 查看代码块填充；
3. 切换代码查看（python\curl\go\java）
4. 复制代码；
5. 打开接口文档。

**预期结果**：

1. 绑定正常；
2. 自动填充参数；
3. 代码切换成功，内容展示无错漏；
4. 复制成功；
5. 新页签打开接口文档，文档内容展示正常，跳转正常。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_016

**标题**：【#60957_接入点_详情页】（功能点 05）正常场景：编辑约束权限正确

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 普通账号、超管；
> 2. 存在业务 / 公共接入点。

**测试步骤**：

1. 编辑业务接入点可改项；
2. 尝试改模型 / ID；
3. 普通账号编辑公共；
4. 超管编辑公共。

**预期结果**：

1. 可改名称 / 描述 / 应用 / 负责人；
2. 无修改入口；
3. 无权限；
4. 可正常编辑。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_017

**标题**：【#60959_接入点_绑定关系】（功能点 01）正常场景：接入点可选择自定义 / 全部权限的 API Key

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已创建正常状态接入点；
> 2. 存在权限为自定义状态为正常和已禁用的 API Key；
> 3. 存在权限为全部状态为正常和已禁用的 API Key；
> 4. 已进入 API Key 绑定接入

**测试步骤**：

1. 在 Key 绑定列表中查看权限为自定义的 Key；
2. 在 Key 绑定列表中查看权限为全部的 Key；
3. 分别勾选两种权限的 Key 并保存绑定；
4. 使用两类 Key 分别调用该接入点。

**预期结果**：

1. 自定义权限状态正常的 Key 展示在可选列表中；
2. 全部权限 Key 状态正常的key展示在可选列表中；
3. 两种 Key 均可成功绑定接入点；
4. 两类 Key 均能正常调用接入点，权限生效。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_018

**标题**：【#60957_接入点_绑定关系】（功能点 02）正常场景：多对多绑定关系正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 多个接入点、多个 Key。

**测试步骤**：

1. 一 Key 绑多点；
2. 一点绑多 Key；
3. Key 权限全部，新增接入点。

**预期结果**：

1. 绑定成功均可调用；
2. 绑定成功均可调用；
3. 自动绑定可调用。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_019

**标题**：【#60959_接入点_状态管理】（功能点 01）正常场景：停止接入点功能正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点正常；
> 2. 有权限账号。

**测试步骤**：

1. 点击停止；
2. 确认；
3. 调用该接入点。

**预期结果**：

1. 弹出确认；
2. 状态变为停止；
3. 返回 403 ENDPOINT_STOPPED。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_020

**标题**：【#60957_接入点_状态管理】（功能点 02）正常场景：启用接入点功能正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点停止；
> 2. 有权限账号。

**测试步骤**：

1. 点击启用；
2. 确认；
3. 调用该接入点。

**预期结果**：

1. 弹出确认；
2. 状态正常；
3. 调用成功。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_021

**标题**：【#60957_接入点_权限】（功能点 01）正常场景：多角色权限校验正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 普通用户、私有租户管理员、超管。

**测试步骤**：

1. 普通用户查看租户下他人业务接入点；
2. 普通用户操作公共接入点。
3. 私有租户管理员查看租户下他人业务接入点；
4. 私有租户管理员查看公共接入点；
5. 超管查看租户下他人业务接入点；
6. 超管查看公共接入点。

**预期结果**：

1. 只可绑定该接入点，其余操作无权限（无法启停、编辑、删除他人接入点，无法查看已绑定API KEY 列表）；
2. 只可绑定该接入点，其余操作无权限（无法启停、编辑、删除接入点，无法查看已绑定API KEY 列表）；
3. 所有操作都可以（可以绑定、启停、编辑、删除，查看已绑定API KEY 列表）；
4. 可以绑定该接入点并查看该租户下绑定的key，其余操作无权限（无法启停、编辑、删除接入点）；
5. 所有操作都可以（可以绑定、启停、编辑、删除，查看已绑定API KEY 列表）；
6.  所有操作都可以（可以绑定、启停、编辑、删除，查看已绑定API KEY 列表）；

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_022

**标题**：【#60957_接入点_API Key 管理】（功能点 01）正常场景：创建 Key 双 Tab 切换正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入 Key 创建页。

**测试步骤**：

1. 切换接入点 / 模型直连；
2. 业务 Tab 多选；
3. 模型 Tab 多选模型；
4. 同时绑定。

**预期结果**：

1. 切换正常，默认接入点；
2. 可多选正常接入点；
3. 保留原有逻辑；
4. 两种调用均生效。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_023

**标题**：【#60957_接入点_API Key 管理】（功能点 02）正常场景：绑定时展示用户可访问的正常状态接入点列表

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入 Key 创建页；
> 2. 公共/业务存在启用和停止的接入点。

**测试步骤**：

1. 切换tab页签，查看展示接入点；
2. 将停止的接入点启用；
3. 将启用的接入点停用。

**预期结果**：

1. 只展示状态正常的接入点；
2. 启用后的接入点绑定成功；
3. 停止后的接入点不展示在列表。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_024

**标题**：【#60957_接入点_API Key 管理】（功能点 03）正常场景：编辑 Key 绑定关系正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. Key 已绑定接入点；
> 2. 进入编辑页。

**测试步骤**：

1. 取消勾选接入点；
2. 切换为模型直连；
3. 查看详情反向展示；
4. 再切换回接入点；
5. 查看详情反向展示。

**预期结果**：

1. 授权失效；
2. 切换后调用正常；
3. 同步更新；
4. 原先模型直连调用失败，接入点调用成功；
5. 同步更新。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_025

**标题**：【#60957_接入点_API Key 管理】（功能点 04）正常场景：API Key 用量达阈值与超限控制正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已创建带点数限额的 API Key；
> 2. 已绑定可正常调用的接入点；
> 3. 可构造用量达到 80%、100% 场景。

**测试步骤**：

1. 调用接口使 Key 用量达到80%；
2. 进入 API Key 列表 / 详情页查看用量展示；
3. 继续调用使用量达到 100% 超限；
4. 使用该 Key 再次调用接入点。

**预期结果**：

1. 用量正常累计，计费扣减正确；
2. 已用 / 限额进度条变为红色，视觉提醒明显；
3. 用量达到限额，计费不再重复扣减（本期不一定实现，看实际）；
4. 调用被拦截，返回 429 QUOTA_EXCEEDED 错误（本期不一定实现，看实际）。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_026

**标题**：【#60957_接入点_用量统计】（功能点 01）正常场景：用量统计接入点列正常

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 存在调用记录；
> 2. 进入用量统计页。

**测试步骤**：

1. 查看接入点列；
2. 按接入点筛选；
3. 超长 ID / 名称展示；
4. 查看非接入点调用。

**预期结果**：

1. 列正常；
2. 筛选准确；
3. 展示正常；
4. 记录正常。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_027

**标题**：【#60957_接入点_用量统计】（功能点 02）正常场景：普通用户仅看自己 Key 的接入点用量，管理员可看全部

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 存在普通用户 A、租户管理员；
> 2. 用户 A 创建 API Key 并绑定接入点(公共接入点+他人业务接入点)，产生调用用量；
> 3. 其他用户也创建 Key 并使用同样接入点产生用量(公共接入点+他人业务接入点)；
> 4. 已进入用量统计页面。

**测试步骤**：

1. 以普通用户 A登录，进入用量统计；
2. 按接入点筛选，查看用量数据；
3. 切换为租户管理员登录，进入用量统计；
4. 按同一接入点筛选，查看用量数据。

**预期结果**：

1. 普通用户仅看到自己 Key 产生的接入点用量；
2. 普通用户看不到其他用户的 Key 调用记录与用量；
3. 管理员可查看该接入点下所有用户 Key 的总用量；
4. 数据展示正确、租户内用量汇总正常。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_028

**标题**：【#60957_接入点_用量统计】（功能点 03）正常场景：公共接入点用量按调用方 Key 所属租户隔离展示

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 公共接入点被租户 A、租户 B 的 Key 都调用过；
> 2. 分别登录租户 A 普通用户、租户 B 普通用户。

**测试步骤**：

1. 租户 A 用户查看公共接入点用量；
2. 租户 B 用户查看公共接入点用量。

**预期结果**：

1. 租户 A 仅看到本租户 Key 的调用用量；
2. 租户 B 仅看到本租户 Key 的调用用量；
3. 跨租户用量不互通、严格隔离。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_029

**标题**：【#60957_接入点_用量统计】（功能点 04）正常场景：修改接入点数据不影响已有授权 Key 调用 & 历史数据正常展示

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已创建接入点并产生调用记录；
> 2. 已有 API Key 绑定该接入点且可正常调用；
> 3. 已进入接入点编辑页。

**测试步骤**：

1. 修改接入点名称、描述、所属应用、业务负责人；
2. 保存编辑；
3. 使用已绑定的原有 Key调用该接入点；
4. 进入使用记录 / 用量统计，按接入点信息筛选历史数据。

**预期结果**：

1. 接入点基础信息修改成功；
2. 原有授权 Key无需重新绑定；
3. 调用正常返回结果，无报错、无权限异常；
4. 历史调用记录按接入点信息仍正常筛选、展示，历史数据不丢失、统计数据连续。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_030

**标题**：【#60957_接入点_用量统计】（功能点 05）正常场景：接入点切换模型后，计费按切换前后模型单价分别计算

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点已创建，初始绑定模型 X（高单价）；
> 2. 已有授权 API Key 可调用该接入点；
> 3. 模型 X、模型 Y 计费单价不同（X 高，Y 低）；
> 4. 接入点支持切换绑定模型。

**测试步骤**：

1. 使用绑定 Key 调用接入点，产生切换前用量；
2. 在接入点详情将绑定模型从 X 切换为 Y；
3. 再次使用同一 Key 调用接入点，产生切换后用量；
4. 进入用量统计 / 账单明细，查看两段调用的计费单价与费用。

**预期结果**：

1. 切换模型前的调用用量，按模型 X 的单价计费；
2. 切换模型后的调用用量，按模型 Y 的单价计费；
3. 账单明细区分清晰，计费归属正确、不混算、不累错；
4. 总费用 = 切换前费用 + 切换后费用，计算准确。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_031

**标题**：【#60959_接入点_调用校验】（功能点 01）异常场景：接入点停止调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点停止；
> 2. Key 正常。

**测试步骤**：

1. 调用该接入点。

**预期结果**：

- 返回 403 ENDPOINT_STOPPED。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_032

**标题**：【#60959_接入点_调用校验】（功能点 02）异常场景：接入点删除调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点已删除；
> 2. Key 曾绑定。

**测试步骤**：

1. 调用该接入点。

**预期结果**：

- 返回 404 ENDPOINT_NOT_FOUND。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_033

**标题**：【#60959_接入点_调用校验】（功能点 03）异常场景：参数错误调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点 / Key 正常；
> 2. 构造非法参数。

**测试步骤**：

1. 发送错误格式请求。

**预期结果**：

- 返回 400 Invalid request。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_034

**标题**：【#60959_接入点_调用校验】（功能点 04）异常场景：Key 状态异常调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点正常；
> 2. Key 停用 / 删除 / 限流。

**测试步骤**：

1. 停用 Key 调用；
2. 删除 Key 调用；
3. 限流 Key 调用。

**预期结果**：

1. 返回 401；
2. 返回 401；
3. 返回 429。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_035

**标题**：【#60959_接入点_调用校验】（功能点 05）异常场景：不存在接入点调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. Key 正常；
> 2. 传入不存在 ep-xxx。

**测试步骤**：

1. 发送调用请求。

**预期结果**：

- 返回 403 ENDPOINT_NOT_AUTHORIZED。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_036

**标题**：【#60959_接入点_解绑场景】（功能点 01）正常场景：各解绑场景立即生效

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. Key 已绑定接入点。

**测试步骤**：

1. Key 持有人取消勾选接入点保持；
2. 删除接入点；
3. 删除 Key。

**预期结果**：

1. 调用立即失效；
2. 绑定失效，调用失败；
3. 绑定失效，调用失败。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_037

**标题**：【#60959_接入点_异常场景】（功能点 01）异常场景：模型下线调用

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点绑定已下线模型；
> 2. Key 已授权。

**测试步骤**：

1. 调用该接入点。

**预期结果**：

- 返回 404 MODEL_NOT_FOUND。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_038

**标题**：【#60959_接入点_异常场景】（功能点 02）异常场景：无模型直连权限越权

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点绑模型 A；
> 2. Key 仅授权接入点。

**测试步骤**：

1. 直接调用模型 A。

**预期结果**：

- 返回 403 MODEL_NOT_AUTHORIZED。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_039

**标题**：【#60957_接入点_异常场景】（功能点 03）异常场景：跨租户调用隔离

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. A、B 租户有相同 ID 业务接入点；
> 2. A 租户 Key。

**测试步骤**：

1. A Key 调用 接A入点 ID。

**预期结果**：

- 调用成功，用量统计记录正常，不路由到 B 租户。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_040

**标题**：【#60959_接入点_异常场景】（功能点 04）异常场景：模型重发自动恢复

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 模型已绑定接入点,接入点绑定key。

**测试步骤**：

1. 模型下线时调用key；
2. 模型重新发布后调用key。

**预期结果**：

1. 404；
2. 模型自动重新绑定接入点，CURL调用成功。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_041

**标题**：【#60959_接入点_异常场景】（功能点 05）异常场景：模型重发自动恢复 & 切换模型后隔离生效

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 接入点初始绑定模型 A；
> 2. 模型 A 先下线，再重新上线；
> 3. 接入点支持切换模型；
> 4. 存在可用模型 B。

**测试步骤**：

1. 模型 A 下线，调用接入点；
2. 在接入点将绑定从 A 切换为 B；
3. 调用接入点；
4. 模型 A 重新上线；
5. 再次调用该接入点；
6. 尝试直接用该接入点调用模型 A。

**预期结果**：

1. 返回 404 MODEL_NOT_FOUND；
2. 切换模型成功，无报错；
3. 正常调用模型 B成功；
4. 模型 A 恢复上线；
5. 仍然正常调用模型 B，不受 A 上线影响；
6. 无法调用 A，必须重新绑定模型 A才可调用。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_042

**标题**：【#60959_接入点_异常场景】（功能点 06）异常场景：同名接入点删除重建

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 原接入点删除后重建同名 ID接入点；
> 2. 旧 Key 曾绑定原接入点。

**测试步骤**：

1. 旧 Key 调用新接入点。

**预期结果**：

- 调用失败，旧key想调用成功需重绑。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_043

**标题**：【#60959_接入点_异常场景】（功能点 07）异常场景：管理员Key绑定普通用户业务接入点，使用与权限隔离正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 存在普通用户创建的业务接入点（正常状态）；
> 2. 存在管理员创建的 API Key（正常状态）；
> 3. 管理员已将该key绑定普通用户的接入点；
> 4. 普通用户、管理员均已登录。

**测试步骤**：

1. 使用管理员的 Key调用普通用户创建的接入点；
2. 进入用量统计 / 使用记录页，以普通用户身份查看；
3. 以管理员身份查看该接入点的调用记录；
4. 查看计费归属。

**预期结果**：

1. 调用成功，权限正常生效；
2. 普通用户仅可查看自己的调用记录，查看不到本次调用记录；
3. 管理员可查看该接入点全部调用记录，查到本次调用记录；
4. 计费正常计入调用方 Key 所属用户。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_044

**标题**：【#60959_接入点_异常场景】（功能点 08）异常场景：租户 A 的 API Key 调用租户 B 的业务接入点，调用失败

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 租户 A 存在正常 API Key；
> 2. 租户 B 存在正常业务接入点（非公共）；
> 3. 租户完全隔离。

**测试步骤**：

1. 使用租户 A 的 API Key；
2. 调用接口时指定租户 B 的业务接入点 ID；
3. 查看返回结果与日志。

**预期结果**：

1. 接口调用被拒绝；
2. 返回 403 ENDPOINT_NOT_AUTHORIZED 或 404 ENDPOINT_NOT_FOUND；
3. 不会跨租户路由，数据严格隔离。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_045

**标题**：【#60959_接入点_异常场景】（功能点 09）异常场景：网络异常时操作容错正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已登录系统；
> 2. 可模拟断网、弱网、请求超时

**测试步骤**：

1. 断网状态下点击创建接入点；
2. 弱网下提交编辑；
3. 网络波动时执行删除；
4. 网络恢复后重新操作。

**预期结果**：

1. 前端弹出网络异常友好提示，不报错崩溃；
2. 请求不重复提交，防止重复创建；
3. 操作终止，给出明确重试引导；
4. 重新操作可正常执行，数据一致。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_046

**标题**：【#60957_接入点_异常场景】（功能点 10）异常场景：前端防抖不发重复请求

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 已进入接入点列表页；
> 2. 可快速连续操作。

**测试步骤**：

1. 快速连续点击搜索按钮多次；
2. 快速切换 Tab 多次；
3. 快速点击分页按钮多次。

**预期结果**：

1. 只发送1 次请求，无重复请求；
2. Tab 切换防抖生效，无重复接口调用；
3. 分页防抖生效，界面不抖动、不乱跳。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_047

**标题**：【#60957_接入点_异常场景】（功能点 11）正常场景：多语言兼容（中 / 繁 / 英）

**优先级**：P3

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 系统支持简中、繁中、英文三语；
> 2. 已登录，可切换语言。

**测试步骤**：

1. 切换为简体中文，查看全页面文案；
2. 切换为繁体中文，查看全页面文案；
3. 切换为English，查看全页面文案；
4. 操作按钮、提示、错误码文案均校验。

**预期结果**：

1. 简体文案完整、无乱码、无缺失；
2. 繁体文案完整、翻译准确；
3. 英文文案规范、统一、无翻译错误；
4. 所有弹窗、提示、错误信息三语均正常展示。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_048

**标题**：【#60959_接入点_接口】（功能点 01）异常场景：创建接入点异常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 调用/endpoints/create 接口。

**测试步骤**：

1. 普通用户建 PUBLIC；
2. 租户内重复 ID；
3. 与公共 ID 冲突；
4. 字段超长；
5. 绑定无效模型。

**预期结果**：

1. 403；
2. 失败；
3. 失败；
4. 参数错误；
5. MODEL_NOT_FOUND。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_049

**标题**：【#60959_接入点_接口】（功能点 02）异常场景：编辑接入点异常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 调用/endpoints/update 接口。

**测试步骤**：

1. 普通用户编辑他人；
2. 普通用户编辑公共；
3. 编辑不存在 ID。

**预期结果**：

1. 403；
2. 403；
3. 404。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_050

**标题**：【#60959_接入点_接口】（功能点 03）异常场景：启停接口异常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 调用 /stop、/start。

**测试步骤**：

1. 重复停止 / 启用；
2. 普通用户操作公共；
3. 普通用户调用其它用户。

**预期结果**：

1. 400 非法状态；
2. 403；
3. 403。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_051

**标题**：【#60959_接入点_接口】（功能点 04）异常场景：删除接口异常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 调用/endpoints/delete。

**测试步骤**：

1. 普通用户编辑他人；
2. 普通用户编辑公共；
3. 删除不存在 ID。

**预期结果**：

1. 403；
2. 403；
3. 404。

**备注**：Sprint2（历史回溯建档）

---

### TC_S2_052

**标题**：【#60957_接入点_快捷接入冒烟】（功能点 01）正常场景：快捷接入正常

**优先级**：P2

**设计方法**：功能测试

**需求编号**：60957

**前置条件**：

> 1. 进入快捷API接入

**测试步骤**：

1. 创建/选择key
2. 绑定模型
3. 复制代码调用

**预期结果**：

1. key绑定成功，toast提示；
2. 模型绑定成功，toast提示；
3. 代码复制成功，调用成功。

**备注**：Sprint2（历史回溯建档）

---
