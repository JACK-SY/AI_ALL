# 模型管理

| 属性 | 值 |
|------|-----|
| 一级菜单 | 模型管理 |
| 优先级 | P0（模型库）/ P1（资源池默认启用策略） |
| 主要用户 | 平台管理员 |
| 需求来源 | F1 平台资源池与租户资源管理、F9 模型发布与默认启用策略、F11 模型供应商 MODEL_ID 变更影响治理 |

## 模块定位与职责

模型管理是平台管理员的**模型资产维护中心**，负责维护模型库的完整信息、管理模型供应商和纳管模型实例、配置资源池默认启用策略，以及处理供应商 MODEL_ID 变更的影响治理。它是模型广场和资源管理的数据源头。

核心职责：决定"平台有什么模型"和"模型怎么分配给租户"。

## 核心概念

| 概念 | 定义 | 推荐用词 | 避免用词 |
|------|------|---------|---------|
| 模型库 | 平台全部纳管模型的资产管理中心 | 模型库 | 模型授权事实源 |
| 模型供应商 | 提供模型服务的上游供应商 | 模型供应商 | 渠道（面向用户时） |
| 纳管模型 | 平台日正式注册并在模型库中维护的模型 | 纳管模型 | 模型实例（当前基线） |
| 默认启用策略 | 资源首次进入租户资源池时的默认启用状态 | 默认启用策略 | 平台加入资源池后一律自动开放 |
| 供应商来源 | 模型实际来源的供应商信息 | 供应商来源 | 来源（单独使用） |
| 模型成本归属 | 模型成本的分类方式（平台成本/客户自有） | 模型成本归属 | 按供应商名称推断 |
| 平台成本模型 | 平台提供的、成本由平台承担的模型 | 平台成本模型 | 我们提供的模型 |
| 客户自有模型 | 客户自行部署和维护的模型 | 客户自有模型 | 客户自己的模型 |

## 角色权限矩阵

| 页面 | 普通用户 | 租户管理员 | 平台管理员 |
|------|---------|-----------|-----------|
| 模型库 | 不可见或只读 | 不可见或只读 | 可见、可编辑 |
| 模型供应商 | 不可见 | 不可见 | 可见、可编辑 |
| 模型实例 | 不可见 | 不可见 | 可见、可编辑 |
| 资源池默认启用策略 | 不可见 | 不可见 | 可见、可操作 |

## 页面字段清单

### 模型库

| 字段 | 说明 |
|------|------|
| modelId | 模型标识 |
| providerName | 供应商名称 |
| 能力标签 | 模型能力分类（对话/嵌入/重排/图像/语音等） |
| 上下文窗口 | 最大上下文 Token 数 |
| 上下架状态 | 已上架/已下架 |
| 部署状态 | 已部署/未部署 |
| 来源渠道 | 供应商渠道标识和名称 |
| 描述 | 模型功能说明 |
| 操作 | 编辑上架信息、上下架 |

### 模型供应商

| 字段 | 说明 |
|------|------|
| 供应商 ID | providerId |
| 供应商名称 | providerName |
| 渠道类型 | official/custom |
| 状态 | 启用/禁用 |
| 上游地址 | 供应商 API 端点 |
| API Key | 加密存储，不展示原文 |
| 支持模型列表 | 该供应商下可用模型 |
| 操作 | 编辑、禁用、查看影响 |

### 模型实例（纳管模型）

| 字段 | 说明 |
|------|------|
| 实例 ID | 唯一标识 |
| modelId | 模型标识 |
| providerId | 供应商 ID |
| providerName | 供应商名称 |
| 状态 | 启用/禁用 |
| 能力参数 | 输入输出模式、Token 限制 |
| 操作 | 编辑、上下架 |

### 资源池默认启用策略

| 字段 | 说明 |
|------|------|
| 资源类型 | 模型/公共接入点 |
| 资源名称 | modelId 或接入点名称 |
| 目标租户 | 分配的租户列表或全部 |
| 默认启用状态 | 默认启用/默认关闭 |
| 操作 | 编辑策略 |

## 核心业务规则

### 模型发布流程（F9）

1. 平台管理员确认纳管模型信息（模型ID、供应商来源、能力参数、健康状态）
2. 选择模型进入哪些租户的平台资源池，设置默认启用状态
3. 选择是否生成公共接入点（默认不勾选）；如勾选→配置接入点名称、路由策略、状态、加入租户资源池范围和默认启用状态
4. 可选套用 AI 安全护栏推荐配置（默认不启用，启用后默认观察模式）
5. 提交→资源进入对应租户的平台资源池→是否立刻对普通用户可见取决于默认启用策略和租户管理员后续配置

### 默认启用策略规则

1. 纳管模型发布前必须明确是否加入租户平台资源池
2. 加入时必须设置默认启用状态
3. 默认启用策略只影响资源首次进入租户资源池，不覆盖租户管理员手动配置
4. 生成公共接入点必须由平台管理员显式确认
5. 未勾选不自动创建接入点
6. 公共接入点生成后可独立加入或移出租户资源池
7. 公共接入点进入租户资源池不要求租户同时启用背后候选模型

### MODEL_ID 变更影响治理规则（F11）

1. 接入点候选模型绑定以 `providerId + modelId` 为准，不因供应商或渠道配置变更自动切换
2. 编辑 MODEL_ID、模型列表、模型映射、上游地址前，系统应提供影响分析
3. 影响分析必须覆盖公共接入点和业务接入点
4. 影响分析至少展示：受影响接入点数量、ID、名称、类型、候选位置、路由角色、优先级、权重、归属租户、当前状态
5. 候选变更后不可用时接入点进入需重新绑定或异常状态；不产生 API Key 资源关闭失效绑定
6. 同一候选后续恢复可用时接入点可继续调用，不要求重建 API Key
7. 切换供应商/模型/渠道必须编辑接入点路由候选重新同步
8. 接入点编辑页必须支持将受影响候选直接替换为新的 `providerId + modelId`
9. 供应商/渠道变更确认页只提示影响范围，不提供批量重绑入口
10. 直接重新绑定时应保留候选在路由中的位置、角色、优先级和权重
11. 公共接入点由平台管理员处理影响；业务接入点让创建租户管理员看到候选异常
12. 本期不做业务接入点候选异常的主动通知
13. 变更、影响确认、候选异常和恢复均应记录审计日志

## 状态流转

### 纳管模型生命周期

```
注册 → 上架 → 分配到资源池 → 租户启用/关闭 → 下架
```

### 供应商变更影响流转

```
正常候选 → 编辑触发影响分析 → 确认保存 → 候选不可用（需重新绑定/异常） → 人工重新绑定 → 同步转发侧 → 恢复可用
```

## 与其他模块的上下游关系

| 方向 | 关联模块 | 关系说明 |
|------|---------|---------|
| 下游 | 模型广场 | 提供模型资产数据，决定哪些模型可以被展示 |
| 下游 | 资源管理 | 决定哪些模型/接入点进入租户资源池及默认启用策略 |
| 下游 | 接入点管理 | 提供候选模型数据，公共接入点绑定的候选模型来源于模型库 |
| 双向 | 接入凭证 | 模型上下架影响 API Key 绑定有效性；供应商 MODEL_ID 变更触发影响分析 |
| 双向 | 调用观测 | 模型相关信息写入调用明细事件；用量统计可按模型维度聚合 |

## 关键决策编号及结论

| 决策编号 | 结论 | 影响 |
|---------|------|------|
| D-04b | 指定租户发布不再作为主路径 | 资源池分配是主路径 |
| D-04g | Studio 维护模型库，Ops 维护资源池和调用治理 | 团队边界清晰 |
| D-04j | 平台可设置默认启用策略 | 策略只影响首次 |
| D-31a | MODEL_ID 变更不自动切换候选 | 需手动重新绑定 |
| D-31b | 业务接入点候选异常不主动通知 | 租户管理员需自查 |
