# 模型观测

| 属性 | 值 |
|------|-----|
| 一级菜单 | 模型观测 |
| 子菜单 | 用量统计 |
| 优先级 | P0 |
| 主要用户 | 普通用户、租户管理员、平台管理员 |
| 需求来源 | F5 调用日志事实流、F6 用量视角与调用资格与用量限额 |

> **旧文档名**：调用观测.md（菜单已重命名为"模型观测"，调用日志已整合进用量统计子页面）

## 模块定位与职责

模型观测是模型调用的**可观测性中心**，当前子页面"用量统计"同时承载调用日志明细查询和多维度用量聚合分析两个职能。两者共同支撑排障、审计、成本治理和计费核对。

核心职责：让每一次模型调用"看得见、查得到、算得清"。

## 核心概念

| 概念 | 定义 | 推荐用词 | 避免用词 |
|------|------|---------|---------|
| 调用明细事件 | 一次模型调用尝试的不可变事实记录 | — | — |
| 事实来源 | Kafka 是日志/用量/告警/审计的唯一事实来源 | — | — |
| 用量视角 | 不同角色看到不同范围的用量数据 | — | — |
| 调用资格 | 租户是否能调用生产模型的本地判断依据 | — | — |
| 实际命中模型 | 接入点路由实际到达的纳管模型 | — | — |
| 入口无关成本限额 | 无论直接调用还是接入点调用，命中平台成本模型就计入同一限额 | 入口无关成本限额 | 接入点专属限额、模型直调专属限额 |
| 路由后限额校验 | 先完成接入点路由校验，再判断实际命中模型的成本归属 | 路由后限额校验 | 接入点级统一限额判断 |

## 角色权限矩阵

| 页面区域 | 普通用户 | 租户管理员 | 平台管理员 |
|---------|---------|-----------|-----------|
| 概览卡片 | 我的用量 | 本租户用量/我的用量 | 全平台/指定租户/我的用量 |
| 统计图表 | 维度分析（用户维度仅本人） | 全维度分析 | 全维度分析（含跨租户） |
| 调用日志明细 | 本人调用 | 本租户调用，可切"本人" | 全平台/指定租户/本人 |
| 明细导出 | 本人数据导出 | 本租户数据导出 | 全平台数据导出 |

> 页面**必须**明确展示当前视角

## 页面字段清单

### 用量统计 — 概览卡片

| 指标 | 说明 |
|------|------|
| 总请求数 | 所选时间范围内调用总次数 |
| 总 Token (M) | 输入+输出 Token 合计（百万）；展开可见输入/输出分项 |
| 平均耗时 (s) | 平均响应耗时；展开可见 P95/P99 |

> 实测补充（2026-07-08）：页面标题为"大模型LLM"，支持日期范围选择器。

### 用量统计 — 维度分析

| 模块 | 说明 |
|------|------|
| 模型分布 | 按模型的请求量/Token 分布列表 + Token 使用趋势图 |
| API Key 分布 | 按 API Key 的请求量/占比分布列表 |
| 用户调用排行 | 按用户的请求量/Token 排行表（租户侧）或租户+用户双排行（平台侧） |

### 用量统计 — 调用日志明细

| 字段 | 说明 |
|------|------|
| 用户名称 | API Key 创建人 |
| API 密钥 | 凭证名称（脱敏） |
| 供应商 | 模型供应商 |
| 模型 | 实际命中模型标识 |
| 接入点（名称 / ID） | 关联的接入点名称与 ID |
| 端点 | API 路径（如 /v1/chat/completions） |
| 状态 | 成功/失败/被拦截 |
| TOKEN | 输入↓/输出↑ Token 数 |
| 首 TOKEN | 首 Token 延时 |
| 耗时 | 总响应耗时 |
| 时间 | 调用时间戳 |
| 操作 | 查看详情 |

> 实测补充（2026-07-08）：平台侧的租户用量监控额外包含 User-Agent 和 IP 两个字段；租户侧不可见。

### 调用日志详情抽屉

| 区域 | 字段 |
|------|------|
| 基本信息 | 所有列表字段扩展值 |
| 路由信息 | 路由意图、是否降级、复杂度请求/解析/是否默认 |
| 错误信息 | error_code、error_message、retry_result |
| 安全护栏 | 检测项、风险等级、处置方式、命中规则 |
| 请求/响应摘要 | 截断展示（如需完整内容走审批流程） |

## 核心业务规则

### 调用日志规则（F5）

1. 每次调用尝试都必须有可追踪记录
2. 调用明细事件优先写入 Kafka，message key 为 `tenant_id:api_key_id`
3. 事件使用 `event_id` / `request_id` 做幂等
4. ES 消费者写入月度索引 `call_log_YYYYMM`
5. 用量消费者更新统计表
6. 告警消费者处理异常和安全护栏提醒
7. OM 计量消费者扣除或扣点
8. 前置校验失败（无资格、余额不足、Key 无效）不请求上游但仍在日志中记录
9. 实际命中模型、路由意图、是否降级、复杂度信息必须写入事件

### 用量视角规则（F6）

1. 普通用户默认"我的用量"，不可切换
2. 租户管理员默认"本租户用量"，可切换到"我的用量"
3. 平台管理员默认"全平台用量"，可切换到指定租户或"我的用量"
4. 公共接入点命中租户未直接启用模型时，用量统计仍可按实际命中模型筛选和聚合
5. 未启用模型筛选项不提供模型详情跳转、绑定或创建 API Key

### 调用资格规则（F6）

1. 具备以下任一条件视为有调用资格：有效 OM 点数包、试用期、调用白名单
2. 无资格时禁止调用生产模型
3. 点数不足时调用前置校验失败
4. OM 状态不在每次调用时远程查询，通过事件/定时任务同步到本地
5. OM 消费失败不回滚已返回的响应，后续通过重试/对账/运营处理

## 状态流转

### 调用事件状态

```
请求到达 → 前置校验 → 路由判定 → 上游调用 → 响应返回 → 事件写入Kafka → 消费入库
                           ↓
                    校验失败（仍记录事件）
```

## 与其他模块的上下游关系

| 方向 | 关联模块 | 关系说明 |
|------|---------|---------|
| 上游 | 基础设置 | API Key 是日志和统计的核心维度 |
| 上游 | 接入点管理 | 接入点是路由和降级信息的来源 |
| 上游 | 治理设置 | OM 点数包/资格状态影响调用结果 |
| 上游 | 运营管理 | 模型配置的计价信息可关联到用量统计；租户用量监控共享底层日志数据 |
| 下游 | 模型广场 | 用量统计中的模型维度聚合结果 |
| 下游 | 模型管理 | 日志中命中模型的供应商归属信息 |

## 关键决策编号及结论

| 决策编号 | 结论 | 影响 |
|---------|------|------|
| D-09 | 调用日志命中模型不扩大模型级授权范围 | 未启用模型不提供跳转 |
| D-13 | 用量统计可按实际命中模型聚合 | 即使租户未启用该模型 |
| D-32 | 调用前置校验只查本地状态 | 安全侧处理缓存延迟 |
| D-33 | 调用明细事件优先进入 Kafka | 事件幂等是关键 |

---

## 测试用例参考

本模块相关测试用例请参考：[测试用例/S4测试用例.md](../测试用例/S4测试用例.md)

| 需求编号 | 用例范围 |
|---------|---------|
| F5 | 调用日志存储治理相关用例 |
| F6 | 用量视角与用户级Token限额相关用例 |
| F14 | 用量统计多维度查询与调用日志展示增强相关用例 |
