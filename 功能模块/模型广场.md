# 模型广场

| 属性 | 值 |
|------|-----|
| 一级菜单 | 模型广场 |
| 优先级 | P0 |
| 主要用户 | 普通用户、租户管理员、平台管理员 |
| 需求来源 | F1 平台资源池与租户资源管理、F9 模型发布与默认启用策略 |## 模块定位与职责

模型广场是面向所有用户的**模型发现入口**，展示经过租户资源启用配置过滤后的可用模型。它本身**不维护**模型资产，只负责"展示什么"——数据来源于模型库（模型管理 → 模型库），可见范围受租户资源启用配置控制。除模型列表外，还承载精选案例展示和新手快速指引。

核心职责：让用户快速发现、了解并体验可用模型。

## 核心概念

| 概念 | 定义 |
|------|------|
| 模型列表 | 普通用户视角：只展示本租户已启用且可调用的模型 |
| 模型详情 | 展示模型能力、供应商来源摘要、可用状态和模型级调用方式 |
| 实际命中模型 | 公共接入点路由到的具体模型，普通用户在模型广场不一定可见 |

## 角色权限矩阵

| 页面 | 普通用户 | 租户管理员 | 平台管理员 |
|------|---------|-----------|-----------|
| 模型列表 | 可见本租户已启用模型 | 可见本租户已启用模型，可跳转基础设置调整 | 可见全平台模型 |
| 模型详情 | 可见已启用模型的概览/调用方式 | 额外可见本租户资源启用状态 | 额外可见供应商、实例等 |
| 精选案例 | 可浏览 | 可浏览 | 可浏览 |
| 快速指引 | 可点击跳转 | 可点击跳转 | 可点击跳转 |

## 页面字段清单

### 首页（精选模型模块）

| 区域 | 说明 |
|------|------|
| 欢迎横幅 | "欢迎来到模型广场 · 精选模块" |
| 描述 | "聚合精选模型、精选案例与入门快速指引，坐拥海量优质模型资源，全覆盖精调、在线体验、API 调用、一键部署全能力" |
| 大模型/小模型切换 | Tab 切换展示大模型与小模型列表 |
| 精选模型卡片 | 模型名称、能力标签（文本生成/多语言/上下文长度）、供应商、更新时间、"立即体验"入口 |
| 全部模型入口 | "全部模型 >" 跳转到完整模型列表 |

### 精选案例区域

| 区域 | 说明 |
|------|------|
| 案例列表标题 | 精选案例 |
| 案例切换标签 | 如"智能图纸检测与解析"/"目标检测与识别"/"轻量化时序预测" |
| 案例详情 | 痛点/方案/价值描述 + 关键指标展示 |

### 快速指引区域

| 区域 | 说明 |
|------|------|
| 指引列表标签 | 如"模型数据集维护"/"模型训练部署"/"模型评测"/"第三方模型上架" |
| 指引详情 | 功能描述 + 模块优势 + "立即体验"按钮 |

> 实测补充（2026-07-08）：模型广场默认展示"精选模块"首页，页面路径 `/model-space/featured-models`。

### 模型列表

| 字段 | 说明 |
|------|------|
| modelId | 模型标识 |
| providerName | 供应商名称 |
| 能力标签 | 模型能力分类 |
| 启用状态 | 已启用（仅租户管理员可见已关闭模型） |
| 操作 | 查看详情 |

### 模型详情页签

| 页签 | 内容 |
|------|------|
| 概览 | 模型描述、能力参数、上下文窗口、输入输出模式 |
| 供应商来源 | 供应商名称、来源摘要（不暴露供应商密钥和内部配置） |
| 接入方式 | 模型直调 URL、可用公共接入点列表（跳转） |
| 资源状态 | 租户管理员/平台管理员可见：启用开关、资源所属资源池名称 |
| 调用观测 | 跳转调用日志和用量统计 |

## 核心业务规则

1. 普通用户只展示本租户**已启用**且可调用的模型；关闭模型对普通用户不可见
2. 模型广场不作为资源池或启用配置的**事实源**——事实源在资源管理
3. 公共接入点路由命中租户未直接启用模型时，模型广场不展示该模型，也不提供跳转绑定或创建模型级 API Key
4. 关闭模型后，模型广场即时不展示，API Key 不能新增绑定该模型，直接调用 `model=M` 被拒绝
5. 平台管理员可查看全平台模型，包括未分配到任何租户资源池的模型

## 状态流转

模型在模型广场的可见性状态：

```
平台资源池已分配 + 租户已启用 → 可见
平台资源池已分配 + 租户已关闭 → 不可见（租户管理员可见灰色标记）
平台资源池未分配 → 不可见
```

## 与其他模块的上下游关系

| 方向 | 关联模块 | 关系说明 |
|------|---------|---------|
| 上游 | 模型管理 → 模型库 | 模型资产数据的唯一来源 |
| 上游 | 接入点管理 | 公共接入点的代表模型摘要来自模型库数据 |
| 下游 | 基础设置 | 模型详情提供"创建模型级 API Key"跳转入口 |
| 下游 | 模型观测 | 模型详情提供"查看调用日志/用量"跳转入口 |
| 下游 | 体验中心 | 精选模型卡片提供"立即体验"跳转入口 |

## 关键决策编号及结论

| 决策编号 | 结论 | 影响 |
|---------|------|------|
| D-04c | 不新增普通用户侧"模型开通"菜单 | 模型广场不提供自助开通功能 |
| D-04f | 模型广场只展示租户已启用模型 | 过滤逻辑依赖租户资源启用配置 |
| D-09 | 调用日志命中模型不扩大模型级授权范围 | 未启用模型不提供绑定/跳转 |

---

## 测试用例参考

本模块相关测试用例请参考：[测试用例/S4测试用例.md](../测试用例/S4测试用例.md)

| 需求编号 | 用例范围 |
|---------|---------|
| F1 | 平台资源池与租户授权范围相关用例（模型可见性控制） |
