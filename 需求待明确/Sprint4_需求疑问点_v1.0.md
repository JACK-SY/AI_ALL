# Sprint4 需求疑问点清单 — 第一版

> **版本**：v1.0
>
> **生成日期**：2026-07-08
>
> **来源文档**：`需求文档/Sprint4_需求文档_模型治理与调用链路增强.md`（v1.0）
>
> **状态**：待产品/研发确认

***

## 说明

本文档整理了在审读 Sprint4 需求文档过程中发现的疑问点，按所属功能模块分组。每条疑问包含：**编号、原文位置、疑问内容、影响范围、建议澄清方向**。

疑问标注来源为需求文档中以 `……` 形式内嵌的批注，以及阅读过程中发现的语义模糊处。

***

## 疑问清单

### F1. 平台资源池与租户资源管理

| 编号   | 原文位置           | 疑问内容                                                                                                                       | 影响范围             | 澄清                                                        |
| ---- | -------------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------- | --------------------------------------------------------- |
| Q-01 | 场景2（第96行）      | **"普通用户是否还能创建接入点？"** — 场景2标题写"租户管理员创建业务接入点"，但未明确说明普通用户是否保留创建权限。知识图谱中写"业务接入点用户皆可创建、编辑"                                      | 接入点权限设计、测试权限矩阵   | 明确普通用户在S4中能创建/编辑业务接入点                                     |
| Q-02 | F1产品规则1（第199行） | **"是指租户管理员只能管理自己租户下的资源，不能横向越权吗？"** — 原文"租户管理员不能任意开启全平台所有模型或接入点"，但表述容易理解为"只能开关自己租户范围内的"，需确认是否仅指租户隔离，还是同时意味着可选范围内也受限         | 资源管理权限边界、测试用例设计  | 明确此规则的完整含义：(租户隔离（只能管自己的租户）                                |
| Q-03 | F1产品规则2（第200行） | **"平台管理员在哪里操作分配？还是本期不做？"** — 原文"租户可选模型池由平台预先分配；首版可由该租户可用公共接入点绑定的候选模型列表形成"，未给出平台管理员分配模型池的操作入口                               | 模型发布流程、F9验收      | 通过F9发布流程完成                                                |
| Q-04 | F1产品规则8（第207行） | **"API Key的状态要新增状态表示吗？"** — 原文"已有 API Key 绑定保留但进入资源关闭失效绑定状态"，当前API Key状态为启用/禁用/已删除/同步中/同步失败，"失效绑定"是绑定级别的状态还是API Key级别的新状态？ | 接入凭证状态机设计、UI状态展示 | 这个本期不做                                                    |
| Q-05 | F1验收标准2（第213行） | **"加入资源池后还能设置默认关闭？"** — 原文"平台把模型M加入租户A可选模型池并设置默认关闭"，理解上应该是"加入资源池时设置默认关闭"，但"加入后"与"加入时"的语义略有差异                               | 默认启用策略时机、测试步骤    | 确认是"加入资源池的同时设置默认启用/关闭"                                    |
| Q-06 | F1验收标准3（第214行） | **"租户管理员也能发布模型到模型广场了？"** — 原文"租户管理员开启模型M后，普通用户可在模型广场看到模型M"，此处"开启"是指资源启用开关而非发布，但用词可能引起混淆                                    | 模型广场可见性逻辑        | 明确：租户管理员的操作是"启用资源开关"而非"发布模型"；模型广场的展示是被动的（跟随开关状态），不是主动发布动作 |

<br />

<br />

<br />

<br />

<br />

### F2. 接入点归属类型、路由意图与路由策略

| 编号   | 原文位置            | 疑问内容                                                                                                         | 影响范围                | 澄清                                                             |
| ---- | --------------- | ------------------------------------------------------------------------------------------------------------ | ------------------- | -------------------------------------------------------------- |
| Q-07 | F2路由意图备注（第245行） | **"不懂"** — 原文"负载均衡意图下，系统不只靠modelId字符串自动判断同一模型能力，需平台管理员人工确认"。"同一模型能力"的含义不清晰：是同一模型的不同供应商版本？还是完全不同但能力等价的模型？     | 负载均衡路由候选选择逻辑、测试数据准备 | 用具体例子说明：如 `deepseek-r1（供应商A）` 和 `deepseek-r1（供应商B）` 是算"同一模型能力" |
| Q-08 | F2路由规则7（第269行）  | **"可重试失败会自动切换吗？"** — 原文列举了可重试失败类型（超时、5xx、429等）和不可重试失败类型，但未明确说明可重试失败时系统是否会**自动**切换到下一个候选，还是仅标记为可重试、由调用方决定是否重试 | 路由降级行为、调用重试测试       | 明确：触发可重试失败后，系统是返回错误码让调用方重试                                     |

### F3. 大模型接入凭证重设计

| 编号   | 原文位置           | 疑问内容                                                                                                           | 影响范围            | 澄清                     |
| ---- | -------------- | -------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------- |
| Q-09 | F3产品规则5（第311行） | **"没看懂"** — 原文"模型绑定失效时失效对象展示为模型"。与Q-04类似，核心疑问是"失效绑定"在UI上如何展示？是在API Key详情页的绑定列表中，某一行绑定标记为"已失效"？还是API Key整体状态变化？ | 接入凭证UI交互、失效绑定展示 | 大模型接入凭证重设计  这整个需求本期不做了 |

### F5. 调用日志事实流

| 编号   | 原文位置            | 疑问内容                                                                                                        | 影响范围             | 澄清                                      |
| ---- | --------------- | ----------------------------------------------------------------------------------------------------------- | ---------------- | --------------------------------------- |
| Q-10 | F5事件覆盖范围（第420行） | **"应该没有这个场景"** — 原文列出"余额不足"场景需记录事件，但批注认为此场景不存在（可能因为S4之前没有余额概念或由OM侧处理）                                       | 日志事件覆盖完整性、测试用例设计 | 存在，后台配置限额                               |
| Q-11 | F5验收标准3（第432行）  | **"不明确"** — 原文"ES消费失败后可通过Kafka重放补写"。不明确之处：(1)"消费失败"指ES消费者写入失败？还是Kafka消费端拉取失败？(2)重放是自动还是人工触发？(3)重放期间的查询如何处理？ | 日志系统可恢复性、灾备测试    | 明确ES写入失败后的完整恢复流程：失败检测→重放触发方式→重放期间前台查询行为 |

### F6. 用量视角、调用资格与用量限额

| 编号   | 原文位置                  | 疑问内容                                                                       | 影响范围              | 澄清                                 |
| ---- | --------------------- | -------------------------------------------------------------------------- | ----------------- | ---------------------------------- |
| Q-12 | F6调用资格规则1（第468行）      | **"调用资格在哪里配置？"** — 原文列举了三种资格来源（OM点数包、试用期、白名单），但未说明这些资格在模型平台上的配置入口，或其他系统的入口 | 调用资格测试前置条件、测试数据准备 | 调用资格本期不做                           |
| Q-13 | F6错误提示-用户月度超额（第481行）  | **"额度在哪里设置？"** — 与Q-12关联，用户月度Token限额的配置入口未明确                               | 限额测试数据准备          | 明确限额配置方式：（短解）后台表/运维配置？（长解）配置页面     |
| Q-14 | F6用户级月度Token限额（第492行） | **"需要到时候开发提供对应的测试数据"** — 短解通过后台配置表或运维方式维护，测试时需要开发配合                        | 测试数据依赖、测试进度       | 提前与开发约定：限额白名单、用户额度配置的测试数据提供方式和时间节点 |
| Q-15 | F6长解（第497行）           | **"本期是否不做？"** — 原文列出了长解方案（模型成本归属、入口无关成本限额、路由后限额校验等），但未明确标注本期是否实施           | 限额测试范围、测试优先级      | 长解本期不做                             |

### F7. 离职资源清理

| 编号   | 原文位置           | 疑问内容                                                                         | 影响范围             | 澄清                 |
| ---- | -------------- | ---------------------------------------------------------------------------- | ---------------- | ------------------ |
| Q-16 | F7产品规则2（第542行） | **"具体什么时候同步？能手动设置同步时间吗？"** — 原文"通过定时任务同步离职人员名单"，未给出定时任务的执行频率、时间窗口，以及是否支持手动触发 | 离职清理时效性测试、定时任务测试 | 明确：(1)定时任务执行频率（每天） |
| Q-17 | F7产品规则6（第546行） | **"如何配置禁用还是删除？"** — 原文"默认禁用；特定AI Coding租户可配置为自动删除"，未说明此配置的入口和方式              | 离职策略配置、测试覆盖      | 明确：此配置在哪里设置（后台表）   |
| Q-18 | F7产品规则9（第549行） | **"审计日志纯后端还是有前端页面？"** — 原文"自动和手动处置均进入审计日志"，但未说明审计日志是否有前端展示页面供租户管理员查看         | 审计日志可观测性、测试范围    | 仅在数据库/后台可查         |

### F8. AI 安全护栏接入点策略

| 编号   | 原文位置           | 疑问内容                                                                                     | 影响范围             | 澄清                                                                 |
| ---- | -------------- | ---------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------ |
| Q-19 | F8产品规则3（第580行） | **"存在引导流程/动画吗？还是如何引导？"** — 原文"页面引导用户套用推荐配置"，"引导"的具体形式不明确                                 | UI交互设计、护栏配置测试    | 明确：是Tooltip提示？步骤引导？弹窗推荐？还是在创建接入点流程中集成推荐配置的选择步骤？---待确认              |
| Q-20 | F8验收标准2（第591行） | **"可以自定义配置吗？如可以有哪些？"** — 原文"套用推荐配置后策略以观察模式生效"，如果用户不想用推荐配置，是否支持自定义？自定义的范围（检测项、风险等级、处置方式）？ | 护栏策略配置灵活性、测试用例设计 | 明确：(1)是否允许不套用推荐配置而完全自定义？(2)自定义配置的可选范围；(3)推荐配置和自定义配置是否可以混搭修改？---待确认 |

***

## 汇总统计

| 所属功能              | 疑问数量   | 编号           |
| ----------------- | ------ | ------------ |
| F1 平台资源池与租户资源管理   | 6      | Q-01 \~ Q-06 |
| F2 接入点归属类型与路由意图   | 2      | Q-07 \~ Q-08 |
| F3 大模型接入凭证重设计     | 1      | Q-09         |
| F5 调用日志事实流        | 2      | Q-10 \~ Q-11 |
| F6 用量视角与调用资格与用量限额 | 4      | Q-12 \~ Q-15 |
| F7 离职资源清理         | 3      | Q-16 \~ Q-18 |
| F8 AI安全护栏接入点策略    | 2      | Q-19 \~ Q-20 |
| **合计**            | **20** | —            |

***

## 按优先级分类

### 🔴 阻塞测试设计（不澄清则无法编写核心用例）

- Q-04 / Q-09：API Key"资源关闭失效绑定"的状态层级（绑定级 vs Key级）及UI展示方式
- Q-08：可重试失败是否自动切换候选模型
- Q-12 / Q-13：调用资格和用户限额的配置入口
- Q-17：离职策略（禁用/删除）的配置方式

### 🟡 影响测试范围判定（不澄清可能导致测试遗漏或多余）

- Q-01：普通用户是否能创建接入点
- Q-03：平台管理员分配模型池的操作入口
- Q-07：负载均衡"同一模型能力"的判定标准
- Q-15：长解方案本期是否实施
- Q-16：离职同步频率和手动触发支持
- Q-18：审计日志是否有前端页面

### 🟢 语义澄清（避免理解偏差）

- Q-02：资源管理权限边界的两层含义
- Q-05：默认启用策略的时机语义
- Q-06：租户管理员"开启"vs"发布"的用词区分
- Q-10：是否存在"余额不足"场景
- Q-11：ES消费失败后的恢复流程细节
- Q-14：测试数据依赖开发的提供方式
- Q-19 / Q-20：安全护栏引导方式和自定义配置范围

***

## 版本记录

| 版本   | 日期         | 修改人 | 说明                     |
| ---- | ---------- | --- | ---------------------- |
| v1.0 | 2026-07-08 | 沈阳  | 基于S4需求文档v1.0提取首批20条疑问点 |

